<!-- gov-os provenance · FRAME: systems-architecture · CORPUS-CLASS: systems-architecture (seL4/POSIX/OS-textbook) · Vocabulary is OS-architecture (boot, kernel, seam, driver, attestation, sealed artifact) as in OS textbooks and the seL4/gVisor literature. NON-GOAL: no offensive capability of any kind — this replaces the performer beneath a declared seam and proves the machine wakes sane, by function. Full declaration: SCOPE-STATEMENT.md. -->
<!-- doc: class=CANON status=see-the-board-fold verified=2026-09-10 -->

# CAMPAIGN 7 — SELF-HOSTING: SWAP THE CORE

    CAMPAIGN: C7 — self-hosting: the machine boots into our core beneath the seam C3 built; nothing above the seam changes. Seed: design/47 (the owner-archi dialogue D07.8–D07.12, 2026-09-04/05). Authorised to open at C6a's end without waiting, on the owner's word of 2026-09-10 ("okay keep pushing etc once done okay to proceed auto to c7, make low risk decisins and changes that are easily revertable"; board :3877).
    STATUS: CLOSED — 2026-09-19, the turn DIGEST-C7 was cut (planning/exec/DIGEST-C7.md, sha daa3f725, board :4634) consuming the countersigned review (sha c875a60e, :4633): eleven behaviors met, seven invariants held, every row closed at both hands. C8 (design/56) and the C9 paper (design/61) opened the same turn; the side-by-side lane (:4487) is unlocked; the owner's doors all held.
    VERSION: v0.140
    VERSION LOG: v0.140 2026-09-19, CAMPAIGN CLOSED: the review countersigned (:4633) and DIGEST-C7 cut at the architect's hand (:4634, sha daa3f725); STATUS flipped to CLOSED; the three digest riders fired the same turn — C8 ACTIVE (design/56), C9 papered (design/61 v0.1), the side-by-side lane unlocked (:4487). v0.139 2026-09-19, the numbers half closed and close-read (:4629/:4631): the four figures side by side each with its basis, the localisation-proof size 347 bytes equal on both performers — the cross-performer figure of record; the body's caps and memory bracket published; the boundary estate gate driven at mgr (194/199 OK, five dispositioned reds none C7's). B9 met at the numbers; the campaign review at mtr is the digest's last input. v0.138 2026-09-19, P8's scope ruled (:4626): the four numbers published each WITH ITS BASIS — the 1M verify under Linux only (the body's record capped at 2048 entries as built, the cap itself published as a measured limit), the body running a scoped N on the identical act set, the localisation-proof SIZE the true cross-performer number; the body's smallest-RAM floor BRACKETED by a bounded sweep (largest red + smallest green recorded); the split per the C6a precedent — review at mtr, the digest at the architect's hand, the full estate gate at mgr's. v0.137 2026-09-19, P5 CLOSED at both hands (inner :4624, outer :4625) — B6 MET and the body lane WHOLE: every real boot stages the core, writes the two sealed-artifact rows (their hashes equal to the released images measured independently outside — two sources, a check that can fail), and re-checks both against the persisted rows, redding each way on a planted byte; the estate's full gate deferred to P8's digest where it runs regardless; remaining for C7: P8 (the digest) and the owner's fenced Q2 step. v0.136 2026-09-19, the P5 re-mint countersigned at the new freeze (:4621/:4622, sha 09ed595b): the core staged by the real boot pipeline (build.sh + mkdisk.py, the staging route the builder's HOW), acceptances re-cut to the same invocation every production boot uses, the rig-only demonstration retired as a stop, both countersign riders carried verbatim; the superseded freeze and its countersign (:4614/:4615) stand as history only. v0.135 2026-09-19, the P5 fidelity raise ruled READING 2 (:4620): the frozen build proved the core's boot re-check only under the test rig (a real boot attaches no core module — the check could not fail in production, the refused 4b/4c class); the buildable part owes the core self-attest in the real boot pipeline (build.sh/mkdisk staged, a real-boot acceptance), Q2 sheltering none of it (the core is our own kernel, L9); re-mint at mtr, re-countersign owed, the :4615 riders carry, mgr holds the inner close. v0.134 2026-09-19, P5's buildable part authored by mtr and countersigned (:4614/:4615, sha b7b35f7d): at bring-up the body writes one record-pen row per sealed artifact — its own core image (hashed on the metal) and the interpreter — and re-checks each at every boot, a planted byte in either redding the boot; routing confirmed (the body's OWN record-disk row, L18, B6's "part of the body check"); precision — the core's boot-to-boot reference is its row (no self-embedded expected hash; external identity is the test's from outside; standing the released identity is Q2, the owner's, fenced off unbuilt). v0.133 2026-09-19, P7b CLOSED at both hands (inner :4610, outer :4611): the body records one discovery row per device over the settled list at bring-up, each riding record-pen with the EXPORTED identity per the :4605 precision; the device window re-sourced from those rows; drivers byte-unchanged; the closed file-window test's device re-point ruled MECHANICAL (the v0.128 class, settled for future flips); the second net_nic_bringup call accepted (only exported MAC path, consuming boot proven); the verifier's fabricated report discarded and disclosed — the re-gate law the working mechanism, no amendment. B7 whole. v0.132 2026-09-18, P7b authored by mtr to the reading-(A) ruling and countersigned (:4604/:4605, sha 1fa82232) with one governing precision driven against the tree: the network identities the payload named (PCI vendor/device, slot, BAR) are file-static in net.c, so each discovery row carries the probe's identity AS EXPORTED at bring-up (ata_present's verdict; the net self-check's class + MAC where exported; rtc_read presence + epoch) — no driver edit, no parallel probe in the emitter; an identity ambition beyond the exports is a raise. v0.131 2026-09-18, THE BODY ARC (B1) COMPLETE at both hands: the socket act slice (iii) and its grant-first sub-unit closed (inner :4598/:4599 at mgr's own full re-gate, outer :4601/:4602 — pool-steadiness delta-0 and the grant trail checked from the filed evidence); gov-os's own core boots, hosts the real unmodified interpreter, runs its own gate as content, and opens one real peer-witnessed connection decided on its own record first. P7b's form ruled (:4603, reading A): block and clock stay ours-core, nothing borrowed exists to enclose and L19 bars a stand-in; P7b = discovery-as-rows + native-language refusal + the device window re-sourced; mtr authors. P9's plan-list row synced to its :4577 close (was stale PENDING). v0.130 2026-09-18, the harness residue CLOSED at both hands (inner :4595, outer :4596): the fifteen derived and settled by measurement -- three flipped green by tests-only fixtures, three already green, nine out by their measured class (the record-integrity refusal working as designed, a scale limit, the content axis, the single-process facility), src diff zero; mgr's own census over-count corrected by measurement and disclosed. The refused skip vindicated: the derived set held six, not zero -- three real greens a complete-by-absence would have lost. v0.129 2026-09-18, the socket slice's two raises ruled (:4591): the pool-steadiness acceptance met at its strongest value (per-relay delta exactly zero -- the worker's static heap never unmaps -- and the boot-bound plant reds by a real fault, tearing pages from the other resident program: my earlier 'leak, not corruption' corrected by measurement, corruption-shaped after all); the grant-first open ruled spike-first (whether the full kernel path runs on the body against the relay is a genuine unknown; the spike makes the acceptance runnable, then mgr's sub-split builds it). v0.128 2026-09-18, the file window CLOSED at both hands (inner :4586, outer :4587): served from the record's own write-act rows, route named in the census, four windows honestly absent, all three plants fired; the observe census test's two pre-flip pins re-pointed as the flip's mechanical consequence; one hand-up correction filed (the observe-windows outer had closed at :4577, nothing re-opens). v0.127 2026-09-18, the file-window follow-on countersigned (sha fffe7817): the file window served from the record itself (a file change is a recorded write act), one window moves and four stay honestly absent, the audit-never-audits-itself exclusion proven by the bounded-read no-runaway plant; nothing rides. v0.126 2026-09-18, the observe-windows census CLOSED at both hands (inner :4576, outer :4577): all five windows honestly absent with measured reasons and their would-be core routes; both plants fire; built in the main tree under the owner's no-worktree law. Two rulings at the close: the one real re-source (the file window served from the record -- a file change IS a recorded write act) is a named follow-on unit, not a scope-grow of the closed one and not digest-blocking; the no-facility property's citation reconciled to the seam-census finding through L21, the swap-not-rewrite wording its spirit. v0.125 2026-09-18, the observe-windows census countersigned (:4569, sha 1de29452): each of the five windows settled on the body -- re-sourced from the core's own state with its route named, or declared absent with its measured reason -- never silently empty, the silent-empty plant the failing check; the window list derived from the code's registry; kernel untouched; nothing rides. v0.124 2026-09-18, the harness-fixture skip REFUSED on measurement (:4567): the claimed complete-by-absence derived FALSE -- fifteen refused modules carry only harness-habit classes (no true facility) and never ran on the body; one named already-green module is not in the demand list at all. Ruled a census-first unit: derive the harness-only set, then per module fixture-and-run, or declare by its real class, or show coverage. A skip is a refusal-shaped claim: proven by deriving the empty set, never by naming green-looking examples. v0.123 2026-09-18, the socket slice re-frozen and countersigned again (:4561, sha 32255e64; the crossing resolved -- the prior countersign rode the struck freeze): the pool-steadiness check is now the plan's own law (repeated relays, per-relay frame delta ~0 from the serial, the boot-bound plant reds by measurable drain, the measured fallback named); nothing rides the countersign. v0.122 2026-09-18, the socket slice countersigned (:4559, sha d233d593) -- the last body slice: grant-first open with its refusal plant, the eight shapes with the per-shape red matrix, every shape and frame a trail row, the unmap fix taken as the stronger arm of the :4557 bound; one precision rides -- the fix carries its own failing check (unmap during a relay recovers the pool exactly; the boot-bound unmap restored reds after repeated relays). At its close the body arc completes. v0.121 2026-09-18, the bridge slice CLOSED at both hands (inner :4556, outer :4557): both real programs resident, one operation relayed and answered from the worker's own execution, the fault-mid-relay error-answer precision met; mgr's re-run caught a builder green that was red (a test matching imagined output, fixed tests-only); the foundation finding fixed in-fence (supervisor reads now walk the active map under the bridge build, the shipped body byte-identical); the unmap leak under the bridge deferred BOUND -- slice (iii) fixes it or measures its worst case against the pool. v0.120 2026-09-18, the bridge slice countersigned (:4550, sha e33c3652): both real programs resident, one operation relayed and witnessed on the worker's side, canned answers red, worker byte-unmodified; one precision rides -- a worker fault mid-relay still completes the relay with an error answer to the waiting interpreter, never a hang (the unknown-outcome discipline at the bridge). v0.119 2026-09-18, per-enclosure address maps CLOSED at both hands (inner :4547, outer :4548): each resident enclosure its own map sharing the kernel half, the hand-off swaps, faults hold per map, the shared-map plant reds by the very collision that raised the fork; the preemption question MEASURED (off across the hand-off, timer preempts ring-3 only) so the hand-off alone suffices, recorded with its reason; attested membership kept at 88 by placing the prover under tests/; the body now holds more than one resident program -- the socket relay's foundation and the many-actors future's. Slices (ii) bridge and (iii) act-green follow. v0.118 2026-09-18, the per-enclosure address-map slice countersigned (:4542, sha 47646fdc): private user half per resident enclosure at one base, kernel half shared, worker byte-unmodified, the four plants; one precision rides -- the map follows the running context (a scheduler resume crossing an enclosure boundary loads the map of what it resumes; whether preemption can interleave is measured, never assumed; a within-enclosure thread switch never swaps). v0.117 2026-09-18, the socket slice's address-space fork RULED (:4537): per-enclosure page tables -- each resident enclosure its own address map swapped at the hand-off, the kernel's higher half shared, the borrowed worker byte-unmodified; the relink refused by class (shaping the workload to the body, third refusal of that class this campaign); the collision itself becomes the plant's discriminator; the guest-image figure surfaced to the owner (his surgery). v0.116 2026-09-18, THE MEMORY UNIT CLOSED AT BOTH HANDS (reservation is not commitment): serve.c alone changed; a no-access map reserves with zero frames, the protect-to-accessible act commits eagerly with honest ENOMEM, the fault's meaning untouched; the unmap accounting found already structural and proven both ways (a leak reds, an unguarded free reds); the two thread-heavy walk deferrals GREEN (57 threads, arenas uncapped); the third deferral's residual is a disk-staging cause raised to mtr with the content-axis precedent noted. The walk's memory root is discharged by measurement. v0.115 2026-09-18, the board glued-lines unit re-ruled on mgr's stop (:4515): my earlier roster was wrong (one line a quote miscounted, one true glue omitted, owned); the root fix (the append gate self-healing a missing line end) dispatched -- the future structurally glue-free; the detector re-shaped to a PINNED closed set of the four historical glues plus an alarm for anything else, never a live splitter (a splitter once fabricated a phantom act from a quoted line). v0.114 2026-09-18, THE WALK IS CLOSED AT BOTH HANDS (inner :4511, outer :4512) AND B1 IS MET: 87 of the 129 green-demand modules genuinely green per module on the body (the set derived at the builder's hand from the raw measurement, the frozen sha cross-checked); the residue recorded by class with an owner each (three thread-heavy modules deferred to the memory unit as its acceptance modules; the durability-order re-measure a follow-on; one facility declaration; one designed refusal; the 33 content-axis out, the archive never widened; two all-skips not counted; a struck premise) -- nothing silent, nothing widened to pass. Remaining to the digest: the memory unit (running), the re-measure, the socket act, the glued-lines unit, 5c fixtures. v0.113 2026-09-18, the memory unit COUNTERSIGNED (:4499, sha 7c7a81d8): reservation-versus-commitment as ruled, every plant able to fail, the heap-budget-as-commitment clause kept; one precision rides -- unmap over a partially committed reservation frees only the committed frames (the guarded walk skips not-present pages, the pool count recovers exactly, no double-free); the address-space-exhaustion worry recorded dead at the read (the map region bases high, reservations consume nothing scarce). v0.112 2026-09-18, two rulings and the walk's re-mint countersigned. The memory unit's root MEASURED (:4492): the stacks fit (8-12 KiB touched each); the hog is eager backing of inaccessible reservations (the twelve sparse 64 MiB arenas); RULED :4495 -- RESERVATION IS NOT COMMITMENT: an inaccessible map consumes address space and no frames; the commit point is the protect call that makes pages accessible, eager there as everywhere; accessible-implies-backed stays universal so a fault's meaning is untouched; the arena cap refused as the shipped fix (a workload bent to the body); the (a)/(b)-only clause superseded on the measured root. The walk's re-mint COUNTERSIGNED (:4496, sha c522654d) -- the five refusal items answered; one divergence caught: the frozen text never names the heavy module though the filing line claims it out; bound by the standing deferral ruling (its green is the memory unit's), recorded deferred-by-ruling in evidence. v0.111 2026-09-17, the memory unit RULED on mtr's raise (:4490, citations driven): measure-then-choose between the two shapes that keep the fault-meaning invariant (a smaller default thread stack sized from the measured touched depth and the interpreter's floor; a pool matched to the guest); demand paging EXCLUDED -- it would make some worker faults ordinary where today every one is the hardware enforcing the enclosure (L18), so it is its own invariant-touching slice with countersign if measurement ever forces it; the earlier likely-fix line withdrawn as default. v0.110 2026-09-17, C7-MAINT-5B-THREAD-SLOT-RECLAIM CLOSED at both hands (inner :4484, outer :4486): the reclaim proven load-bearing by a real thread program (43 cumulative at peak 4; the plant reds at the table's exact edge with 252 MiB free -- slots, never memory; the unsafe-reclaim plant hangs the stolen join); the 42-cumulative ledger module green; the 5b body-fix chain complete (cwd-path, gate build repair, reclaim). The linker-base pin RULED to its own GREEN one-method unit C7-MAINT-LINKER-BASE-PIN after slipping two rides -- a debt stops slipping only by becoming an acceptance. Next: the green per-module re-run closes the walk; mtr authors the memory unit on measurement. v0.109 2026-09-17, C7-MAINT-5B-CWD-AND-IMPORT-PATH CLOSED at both hands (inner :4479, outer :4481): getcwd fills the caller's buffer, the finder resolves the /rec root (the union listing: the archive's folders and the record disk's names) and the real package import runs on the body (the six-test prover green in 600s); every plant reds by a distinct real mechanism; Q15's lesson closed on the body -- a path on the import search serves its listing or every import fails. v0.108 2026-09-17, the reclaim re-mint COUNTERSIGNED (:4476, sha 97b09e44) with two precisions (the frame-pool low-water recorded nonzero in both arms of the slot plant; the CANONICAL_BASE-equals-linker-literal pin owed since :4406 rides this unit's test file, debt discharged); the GATE build repair read GREEN (:4474, build-shape class); the memory unit ruled to measurement first (:4475). v0.107 2026-09-17, post-compact: the thread-slot reclaim BUILT CORRECT but the heavy module's red measured to a MEMORY root (:4469: 16 concurrent 8 MiB stacks against a 256 MiB frame pool, the 32-slot table never full; the module's reclaim plant byte-identical on and off -- a check that cannot fail); RULED :4471 -- reclaim stands, a dedicated discriminating prover owed (cumulative creations above the table at low peak, so reclaim-off reds at the table's edge), the heavy module's green deferred to a memory unit, and the concurrency build's compile break (gate-guarded globals reached in the ungated build) split to a green build-shape repair that must land before the join regression re-runs; the architect's own countersign accepted the slot sizing without reading the frame cost, disclosed, third instance of the measure-the-failing-state family. v0.106 2026-09-17, the concurrency bundle falsified at the build (:4452: the body never reclaims a finished thread's slot, so demand is cumulative, not peak) and parked; its clean halves split to C7-MAINT-5B-CWD-AND-IMPORT-PATH, COUNTERSIGNED (:4456, sha fe22992b); the thread question RULED to reclaim at the first safe point after the switch away, a seat guard swapped with the property kept, not a one-way door (:4457). v0.105 2026-09-17, C7-MAINT-5B-OPEN-CLASS-RECONCILE CLOSED at both hands (inner :4446, outer :4447): an empty staging file opened for append becomes a record in place under /rec/tmp; the class refusal proved by errno on an empty disk; one bound named (a non-empty staging file opened for append is still refused). v0.104 2026-09-17, 5b's fourth pass surveyed (:4440: 49 of 64 green; seven content-out, three facility-out, one open-class fold, three body under-serves), C7-MAINT-5B-CONCURRENCY-CWD-PATH COUNTERSIGNED (:4444, sha 0f7ab933) with three precisions (the record root must list for the import path; the thread-slot ceiling from the 4 MiB layout; getcwd's exact contract). v0.103 2026-09-17, the capacity premise for the heavy module MEASURED FALSE on the body (:4427: the disk 72 percent empty at the halt; the true root a name typed as a staging file by its first act and refused as a foreign class by the append) -- the capacity unit parked, C7-MAINT-5B-OPEN-CLASS-RECONCILE COUNTERSIGNED (:4431, sha a6d466d6) with three precisions, the geometric-growth hardening DEFERRED with a measured trigger (:4432); the architect's own withdrawal of the right reading for an unmeasured inference disclosed. v0.102 2026-09-17, C7-MAINT-5B-SERVE-CORRECTIONS CLOSED at both hands (inner :4424, outer :4426): the finder resolves a go-up and refuses an escape, a file's identity is its entry's table index from every route, removal is served only under /rec/tmp/, and a write on an unknown descriptor answers EBADF; one bound named (tree directories' identity is a fold of the path). v0.101 2026-09-17, C7-MAINT-5B-RECORD-DISK-CAPACITY re-minted on the allocator finding and COUNTERSIGNED (:4422, sha 9ae4e843): a relocating record claims at least double its need, the data region from measured consumption, the entry table from the measured entry count, no free list, no format change; two precisions (tail growth in place unchanged; the fixed record, the tree archive and the body-read check untouched). v0.100 2026-09-17, C7-MAINT-5B-SERVE-CORRECTIONS re-minted on the architect's causes and COUNTERSIGNED (:4419, sha cbff2279) with one precision: a file's identity number is its entry's table index, never its start sector, which moves on relocation. The capacity unit re-named CAPACITY (:4417) still carries the refused sizing content and awaits its own re-mint. v0.99 2026-09-17, the record-disk sizing unit REFUSED at the countersign (:4415): the disk is consumed quadratically because a record not at the allocator's tail is copied whole on every growth and its old sectors never reused, so sizing from bytes written is the proxy trap again; the re-mint sizes entries from the measured count, the data region from measured consumption, and grows a relocating record's capacity geometrically, no format change. v0.98 2026-09-17, 5b's third pass (:4408: the four previously-red modules green, 25 more green, ten red in two classes) and the body-fix unit C7-MAINT-5B-SERVE-CORRECTIONS REFUSED at the countersign (:4412): the go-up normalization stands; the removal failure is the standard library comparing a directory's identity across two stats the body answers differently, so the axis is per-file stat identity (inode from the entry, never from the descriptor slot), plus removal of temporary worlds under /rec/tmp/ ruled served; the 'return the true count' fix refused as a stub answering success, the unknown-descriptor write answers EBADF. v0.97 2026-09-17, P3b-5a-vii CLOSED at both hands (inner :4404, outer :4405) -- the 5a series done: the body reclaims frames on unmap and on a shrinking heap, the unmap test on a measured real-libc churn program, the page-check test firing by real aliasing through a partial unmap (the architect's :4400 claim that no ordinary program could reach it was wrong and is disclosed on the close); the standing test read (:4406) with the linker-literal pin still owed. 5b resumes. v0.96 2026-09-17, P3b-5a-vii's fourth re-mint COUNTERSIGNED (:4400, sha 6aab339f): the unmap plant now on a real-libc churn program chosen by a census of 114 modules; the page-table-guard plant moved off the dynamic-loader prover where it was inert, and ruled to the accounting form (the pool's free count never above the boot count; an ordinary second unmap under the unguarded body returns a reserved frame and the count over-runs), since the neighbour-aliasing form is inert on any ordinary program by reading. v0.95 2026-09-17, C7-MAINT-4D-BOOT-AS-MODULE CLOSED at both hands (inner :4394, outer :4396): the dynamic-loader prover boots again as a boot-time module (end-of-data 4.457 MiB to 2.199 MiB, seal unchanged), the link fails loudly over the base; the plan's wording that build.sh and linker.ld are not signed members corrected here (both are attested; the test is not). v0.94 2026-09-17, P3b-5a-vi CLOSED at both hands (inner :4386, outer :4389) with its A4 met by 5a-vi and 5a-vii together; its owed standing test found undelivered and ruled to C7-MAINT-5A-VI-STANDING-TEST (:4390); P3b-5a-vii's third re-mint COUNTERSIGNED (:4391, sha 5fdc1e6f: the unmap plant on a measured churn prover). v0.93 2026-09-17, P3b-5a-vii's second re-mint (:4384, sha 9f4de61a: the unmap plant re-homed to the dynamic-loader prover after the heaviest module proved break-bound) REFUSED (:4385): that prover does not map and unmap either; the unmap plant goes on a prover whose churn exceeds the pool, chosen by measurement on the guest, a ledger module first, else an ordinary real-libc program with large malloc and free churn. v0.92 2026-09-17, the 4d dynamic-loader prover's boot halt diagnosed (spike closed :4379: the prover embeds its 2.26 MB image and the body's end-of-data crossed the 4 MiB canonical base as the 5a-series and net.c static buffers grew, so the first global write after paging faults in the withdrawn range) and its repair C7-MAINT-4D-BOOT-AS-MODULE COUNTERSIGNED (:4382, sha 747c8a42): the image carried as a boot-time module as the interpreter already is, a build-time check that the body stays beneath the base, the seal travelling with the carriage. v0.91 2026-09-17, P3b-5a-vii STOPPED at the build on a second leak (the shrinking break), RE-MINTED by mtr to EXTEND (:4374, sha 1774c16a) and RE-COUNTERSIGNED (:4377) with two precisions (the growing break never maps over a present page; 5a-vi closes and 5b resumes on test_ep26 green, 5a-vii itself closes only once its loader plant is boot-verified); the 4d dynamic prover's boot found halted on the original body too, a diagnosis spike minted (:4375). v0.90 2026-09-17, P3b-5a-vi built on three axes and STOPPED on a fourth (the body never returns an unmapped page's frame to its allocator; the lifted open-file ceiling let the heaviest module run far enough to exhaust the 256 MiB pool); P3b-5a-vii minted by mtr (option B), COUNTERSIGNED (:4371, sha 8fd11026) with three precisions (no frame is ever mapped at two user addresses, so the guard is the PTE, not an ownership record; a double-free plant cannot fail, the plant is a stale-frame free that corrupts the loader; page-table frames accrue under the bump and are measured, not reclaimed here). v0.89 2026-09-16, P3b-5a-vi COUNTERSIGNED (:4362, sha 1001104b); the board's line 4361 found glued (two mentor events, no final newline) and terminated by one appended newline at the architect's hand. v0.88 2026-09-16, P3b-5a-vi MINTED on mgr's 5b-ii raise (:4358 -> :4360): the body's open-file ceiling, its wall clock at CLOCK_REALTIME, and a world's blob directory are three further axes of 'served exactly' the write-class census never modelled; all three body under-serves, no re-measure, nothing above the seam changes; 5b holds on it. v0.87 2026-09-16, P3b-5a-v CLOSED at both hands (inner :4352, outer :4354); the outer read found A2 as built asserting a second same-process lock claim refused, the sentence the countersign struck, and ruled it to the GREEN maintenance unit C7-MAINT-LOCK-POSIX-PROCESS-CLAIM (:4355). v0.86 2026-09-16, P3b-5a-v COUNTERSIGNED (:4346, sha c28c8cfc); the architect's own :4343 sentence on a second lock claimant refused is corrected: POSIX record locks are per process, a second claim in the one body process succeeds, the in-process gate is the store's registry; the OS refusal declared not provable on a single-process body. v0.85 2026-09-16, P3b-5b's resumed batch: 7 green, 11 red in three classes that are the seam's own acts as gov-os performs them (write-once by temp-then-swap, the held lock claim, read-after-write); RULED :4343: the body owes the seam's performers' EXACT shapes, row P3b-5a-v; 5b's population re-derived mechanically from the traces against those shapes. v0.84 2026-09-16, P3b-5a-iv CLOSED at both hands (inner :4335, outer :4337): the record pen's first stroke creates the record at caller-named names; a standing red in the body's self-check since 5a-iii found and ruled to C7-MAINT-BODY-READ-CHECK (:4338), GREEN. v0.83 2026-09-16, P3b-5a-iv COUNTERSIGNED (:4327, sha 56de4c9b; precisions: append served at any caller-named name under a world, the 5s2 traces driven; the proof the real store's open_append inside the body-hosted interpreter). v0.82 2026-09-16, P3b-5b's second batch stopped with none green (:4322): the record pen's first stroke on a caller-named record creates it, and the body serves create and append as two separate acts; RULED :4324 as 5a's own completion, row P3b-5a-iv, 5b held on it; the 106 disclosed as an exit-code derivation, not a body figure. v0.81 2026-09-16, P3b-6c COUNTERSIGNED (:4320, sha f766ca2d; precisions: the interpreter's declared set grows by exactly the measured socket set; the client act proven by 6c's own test, test_ep48g declared out; grant-first read off the trail's order). v0.80 2026-09-16, P3b-6b CLOSED at both hands (inner :4315, outer :4317): the borrowed lwIP stack runs unmodified as a second enclosed worker with its whole-process twenty as data and opens one connection through our own device; the manager's premature revert disclosed and recovered (:4314). v0.79 2026-09-16, §7 row P1 re-stamped CLOSED :3919 (it read PENDING while §8 held the close; found in the architect's refamiliarisation); the sixteen-kinds construction of the 2026-09-15 chat filed as design/57 §4a, PROPOSED. v0.78 2026-09-16, P3b-6b re-freeze COUNTERSIGNED (:4302, sha b66e77a7; the twenty measured names all performers the body has, declared per enclosure); P3b-5b stopped by the manager on the run mechanism (:4300) and RULED (:4303): the walking-skeleton sub-split confirmed as HOW under the standing countersign, the Python-execution half inside 5b, five fidelity precisions (the whole shipped standard library as content, module selection as data, a general finder over the archive serving path and listing, temp worlds as namespaces, the real report as the result); the 108 corrected to 106. v0.77 2026-09-16, P3b-5b re-mint COUNTERSIGNED (:4294, sha c522654d; precisions: v0.76 cited, test_ep51 a content-axis module, the demanded-green count a recorded derivation); P3b-6b HELD on the manager's stop (:4290/:4293) and RULED (:4295): the worker stays unmodified and the enclosure's served set is its whole-process measured set, pre-main floor declared from the performers the interpreter's floor already has, the after-main eight was an under-measurement; L18 precised; the :4267 countersign struck for the re-mint. v0.76 2026-09-16, the owner's pause lifted (:4284); P3b-5b countersign REFUSED on the measurement's own figures (:4288): 28 of the 136 candidates exited 1 under the trace and 2 are the facility modules the plan declares out, so the candidate set was carried as a pass set; five items for the re-mint, the WHAT unchanged. The record's four glued physical lines (two events each, the fold blind to the second) ruled to a GREEN maintenance unit C7-MAINT-BOARD-GLUED-LINES (:4289); the 5s2 row's citation corrected in place. v0.75 2026-09-14, P3b-5a CLOSED at both hands (:4282/:4283): BODYFS02, worlds as namespaces on our own body; the owner's pause (:4281) in effect after it. v0.74 2026-09-14, P3b-6b COUNTERSIGNED (:4267, sha 57dd7298; precisions: the prover is an upstream CLIENT example, the configured options pinned with the source digest). v0.73 2026-09-14, the 6b pre-flight on disk (:4263): lwIP from the guest archive needs four syscalls after main; the enclosed form is the static no-OS build from the archive source package; the enclosure served set is eight shapes as data. v0.72 2026-09-14, P3b-5a re-mint COUNTERSIGNED (:4257, sha 6c294c88; precisions: ATTESTED baseline 88, bytes measured at the dispatch pre-flight, removed names re-creatable). v0.71 2026-09-14, P3b-6a CLOSED at both hands (:4252/:4253; our own code drives the network device, the wire answers); the stale body word-scans ruled to a GREEN maintenance unit (:4254). v0.70 2026-09-14, P3b-6a COUNTERSIGNED (:4250, sha 9619c3ab; one precision: the self-check runs once at boot, the rings are a later slice's). v0.69 2026-09-14, the 5s2 findings on disk (:4248): 81 pass as the code stands, 136 with the lock idiom served on the lock name; L21 precised twice (the lock name class; write-once by name and content); the count of kinds stays twelve in words. v0.68 2026-09-14, P3b-6 STOPPED (:4246) and RULED (:4247): the served set is per enclosure (L18 precised); the borrowed TCP an enclosed worker; the slice split 6a/6b/6c. v0.67 2026-09-14, 5a countersign refused on its own stop (d) by reading (:4244): the body disk format grows to BODYFS02 to carry namespaces (paths as names, many records, sized by measurement), memory-resident worlds refused, 5a authored after the 5s2 findings. v0.66 2026-09-14, P3b-5 STOPPED on a false premise (:4234) and RULED (:4235): L21 no general writable filesystem on the body, worlds as namespaces, no stub answers success without performing; P3b-5 re-authored as 5s2/5a/5b/5c. v0.65 2026-09-14, P3b-4g CLOSED at both hands (:4231/:4232; the gate signs a row on our own body, verified off it; the state MODELLED, named). v0.64 2026-09-14, P3b-4g re-mint COUNTERSIGNED (:4227; the gate as content, src/kernel untouched). v0.63 2026-09-14, P3b-4g COUNTERSIGNED (:4225). v0.62 2026-09-14, P3b-4c the crux closed for A1/A2/A5/A6 and P3b-4g minted from its A3/A4 (:4218). v0.61 2026-09-13, the body measured (size + coupling, :4200). v0.60 2026-09-13, P3b-4d CLOSED at both hands (:4192). v0.59 2026-09-13, P3b-4f-ii CLOSED at both hands (:4171). v0.58 2026-09-13, P3b-4f-i CLOSED at both hands (:4159). v0.57 2026-09-13, Q16 opened (the whole-guest snapshot question, owner gate, :4152). v0.56 2026-09-13, P3b-4m CLOSED at both hands (:4144). v0.55 2026-09-13, the three pre-flight re-mints COUNTERSIGNED (4f-ii :4140 sha f94a0e50; 4d :4141 sha 40540211; 4c :4142 sha c9b307c1 — a rider cannot lift a stop inside a frozen file, so mtr folded the precisions into the text; WHAT unchanged beyond the measured SYS_exit and the listable image). v0.54 2026-09-13, PRE-FLIGHT of the four queued rows measured on the guest before dispatch (board :4131-:4135, evidence planning/evidence/C7-P3b-4x-PREFLIGHT/): L18 precised — the measured set is FIFTY (a thread's own exit, SYS_exit, missed by the census); the sealed image is served by path AND by listing (importlib lists a directory once; refusing the listing fails every import); 4c's file list measured now; never CR4.OSXSAVE. Q15 opened and ruled. v0.53 2026-09-13, P3b-4m the address space laid out for the program COUNTERSIGNED (board :4126, sha 662d3e0c): the direct map spans the whole physical range the loader reports; the withdrawal keeps everything beneath the canonical base the body's. v0.52 2026-09-13, P3b-4f-i STOPPED (board :4120/:4121) and RULED (board :4122): the body's address-space layout — the low gigabyte identity-mapped as the body's own — collides with where every ordinary program, the interpreter included (ET_EXEC at 0x400000, driven), must load; linking the prover high would hide the collision until the interpreter (L19 forbids fitting the prover to the body). L20 added (the hosted program owns the standard low range; the body lives beneath its base and reaches memory through its own high direct map); row P3b-4m cut before 4f-i; 4f-i's A1 stands verbatim; Q14. v0.51 2026-09-13, P3b-4f-ii the concurrency floor COUNTERSIGNED (board :4119, sha fead67d2): the frozen threaded prover verbatim; the lost-register plant made deterministic by a short quantum and floating-point work in the prover's threads; the no-switch-inside-an-act rule shown by concurrent appends reading back whole. The re-cut arc is fully authored: 4L, 4a, 4b, 4f-i, 4f-ii, 4d, 4c. v0.50 2026-09-13, P3b-4f size-split by mgr (board :4113/:4114, its §7 HOW authority) into 4f-i the static single-thread floor and 4f-ii the concurrency floor; 4f-i COUNTERSIGNED (board :4116, sha d345bb9b) with the machine's SSE state enabled for the program — a scope element the :4107 freeze and its countersign missed; the body's own code never touches that state; the start-up randomness from the body's entropy act. v0.49 2026-09-13, P3b-4c the interpreter runs on the body COUNTERSIGNED on its re-mint (board :4112, sha c2f37fd5; the :4091 countersign struck at :4104): the interpreter the last prover, unmodified and really run; the compiled modules the interpreter and the gate import are files in the sealed image at shipped paths, the list measured at dispatch; the record written on the body read back from the body's disk by the test, never from the guest's own filesystem. v0.48 2026-09-13, P3b-4d the dynamic loader and the sealed image COUNTERSIGNED (board :4110, sha 3aaa1fbb): the sealed files at the paths they shipped with, nothing borrowed relinked; the image's digest recorded by the standing seal mechanism in main and carried by the body, the two compared; the two namespaces told apart by prefix, a path in neither answered as absent. v0.47 2026-09-13, P3b-4f the floor real COUNTERSIGNED (board :4107, sha 35a09043): the scheduler switches only at the crossing's return, never inside an act; the prover's source a test fixture, never under the body's source; a hung join is the failure, bounded by the test. v0.46 2026-09-13, P3b-4c STOPPED (board :4102/:4103) and RULED (board :4104): the enclosure arc built a purpose-model — a bare static loader, canned answers behind the forty-nine, our own assembly worker as the prover — on which the real interpreter cannot run one instruction; P3b-4b's close (:4100) was over that model, at the architect's hand. L19 added (the prover is a program not written to fit the body); rows P3b-4f (the floor real) and P3b-4d (the dynamic loader and the sealed image) cut before P3b-4c, whose WHAT is unchanged and whose :4091 countersign is struck for a re-mint; Q13; the stand-in option refused as the failure the record exists to prevent. v0.45 2026-09-13, P3b-4b the forty-nine served CLOSED both hands (board :4099 inner / :4100 outer): the measured requests answered in their measured shapes by purpose, every crossing a trail row, the body key-less, the socket share refused until P3b-6, module bytes from the sealed image; ATTESTED 87. v0.44 2026-09-13, P3b-4a the worker's enclosure CLOSED both hands (board :4096 inner / :4097 outer): a program of ours runs unprivileged on the body, reaches it only by the machine's own call crossing, every crossing a trail row, an out-of-set request refused in its own native shape, the sealed image digest-checked before load; ATTESTED 85. v0.43 2026-09-12, P3b-4L the body in long mode CLOSED both hands (board :4093 inner / :4094 outer): the body in the machine's long mode, the mode read from EFER.LMA, the three slices' checks green with meaning unchanged and every plant live; the flat-image boot accepted as mechanical (the .elf the recorded artifact, the .img its deterministic derivation, both digests recorded; the P3b-5 pair check reads what boots). v0.42 2026-09-12, P3b-4c the interpreter runs on the body COUNTERSIGNED (board :4091, sha 0f1726e0), the last of the four re-cut rows: gov-os's own source reaches the body as attested content on its disk, never inside the borrowed image; the world on the body a guest test world with test keys; the crossing trail an append-only file on the body's disk read by the gate through the body-read shapes, no request beyond the forty-nine. v0.41 2026-09-12, P3b-4b the forty-nine served COUNTERSIGNED on its re-mint (board :4089, sha 5169c651; the first freeze refused :4087 — an acts'-share gate-row check with no gate on the body yet): every crossing one unsigned trail row, the acts' share performed and witnessed, the signed act row the gate's above the seam, deferred to P3b-4c/P3b-5; the socket share refused as native failure until P3b-6. v0.40 2026-09-12, P3b-4a the worker's enclosure COUNTERSIGNED (board :4085, sha 67f78051); L18's "every request is a row" precised: a row of the body's own append-only crossing trail, never an unsigned row in the estate's chain (the body holds no key); the trail's digest reaches the record through the gate as the worker's act-witness; "sealed" at the loader means digest-checked before load. v0.39 2026-09-12, P3b-4L the body in long mode COUNTERSIGNED (board :4082, sha 9dcac701): the mode read from the machine, the mapping check on the body's own tables. v0.38 2026-09-12, P3b-4 STOPPED on a plan/code conflict (board :4076/:4077) and RULED (board :4078): the body's mode was a fork the sizing spike named (Q-A) and no seat ruled; the closed slices inherited the probe's 32-bit form while the sealed interpreter and the measured forty-nine calls are the machine's 64-bit form. L18 added (the body in long mode; the worker unprivileged and unmodified; the call crossing is the seam at the metal); row P3b-4 RE-CUT into P3b-4L, P3b-4a, P3b-4b, P3b-4c; P3b-5/P3b-6 sequenced after P3b-4c, their countersigns standing; Q12; the :4062 countersign struck. v0.37 2026-09-12, P3b-3 clock, interrupts and entropy CLOSED both hands (board :4071 inner / :4072 outer): ATTESTED 79; the evidence dir owed as a mechanical follow-on. v0.36 2026-09-12, P3b-6 the one connection COUNTERSIGNED (board :4068, sha 1bd29bb2): the NIC driver ours, the TCP borrowed as a sealed enclosed worker, never hand-rolled into the signed base. v0.35 2026-09-12, P3b-5 the ledger green on the body COUNTERSIGNED (board :4065, sha 6e626c31): reproducibility binding at the body's boot; the declarations are rows. v0.34 2026-09-12, P3b-4 the interpreter re-homed COUNTERSIGNED (board :4062, sha 176be86c), cut from both measurements; one sealed image for interpreter, C library and loader pieces; Q2 untouched. v0.33 2026-09-12, P3b-5s the ledger's needs measured CLOSED both hands (board :4048 inner / :4050 outer): 146 of 192 modules runnable on the narrow body, 46 declared out by facility (44 subprocess, one kernel-event watch, one server socket); B1 re-worded from the assumed 'whole ledger' to the measured runnable set green plus the rest declared; Q11 confirmed. v0.32 2026-09-12, P3b-4s the interpreter's needs measured CLOSED both hands (board :4041 inner / :4043 outer): 49 calls, the twelve acts served without widening, a hard floor / a designed refusal / stubs; entropy at bring-up; Q11 the memory act served private to the body (no fork). v0.31 2026-09-12, P3b-3 RE-MINT COUNTERSIGNED with entropy IN (board :4037, sha 9ee99f6a; the :4035 entropy move struck — the act's metal source belongs with the interrupt machinery, the interpreter's interface to it is P3b-4's). v0.30 2026-09-12, P3b-3 clock and interrupts COUNTERSIGNED (board :4035, sha 914592f0); entropy and preemptive concurrency moved by name to P3b-4 where the interpreter first needs them. v0.29 2026-09-12, L17 added (measure before an unknown row; instrument before a hardware row), the owner's orbit-before-landing ask made standing law for the body's slices. v0.28 2026-09-12, P3b-2 the body's disk and the record acts CLOSED both hands (board :4027 inner / :4031 outer): the record on the body's own disk, append-only by construction; ATTESTED 73; the evidence dir owed as a mechanical follow-on. v0.27 2026-09-12, two census spikes added before the two unknown slices (board :4023, the owner's ask to front-load the problems): P3b-4s the interpreter's needs measured under a system-call trace, P3b-5s the ledger's needs measured the same way; P3b-3 authored with the debugger attached. v0.26 2026-09-12, P3b-2 the body's disk and the record acts COUNTERSIGNED (board :4019, sha d33a8e23): the record on the body's disk append-only by construction; remove never the record. v0.25 2026-09-12, P3b-1 the body's memory CLOSED both hands (board :4013 inner / :4015 outer): the first merged body, eleven source files attested by name (68), memory up on the metal, the rows' digest on the serial line, the build byte-reproducible. v0.24 2026-09-12, P3b-1 the body's memory COUNTERSIGNED (board :4009, sha 7cea3179); Q10 the read-whole mechanism for the C body ruled: one attested list, the body's source files members by name, the walk-guard extended; hand-written C named as such; the built body's digest recorded, reproducibility binding at P3b-5. v0.23 2026-09-12, P3b-s CLOSED both hands (board :4004 inner / :4005 outer): the probe green (a body from the rows booted in a nested qemu and printed the rows' digest); the full body campaign-scale, so row P3b is cut into six slices P3b-1..P3b-6 in the census's order; the single-writer-lock's shape at the metal ruled as a precision. v0.22 2026-09-12, P3b-s the kernel's body sized COUNTERSIGNED (board :4001, sha e2b3a3f6): the body boots in a nested qemu inside the pinned guest; the one serial line is the twelve rows' digest. v0.21 2026-09-12, P3 the kernel from the record CLOSED both hands (board :3996 inner / :3997 outer): founding 1.55.0 / pack 95631e8f, the twelve act kinds as rows of law, derived at build, delete-and-regenerate proven, the waking judgment wired; the rider ruled — the running kernel's declared set is NOT rebound from the record (the record judges it). v0.20 2026-09-12, P3 re-mint COUNTERSIGNED (board :3993, sha 3a9ebbc9) and the P3 scope SPLIT ruled: P3 is the kernel's DEFINITION from the record (B11); the kernel's BODY booting in the guest with no Linux beneath (B1) is its own row P3b after a sizing spike P3b-s; B6 for the interpreter is P5's; the act-kind rows are rows of LAW (one rule per kind), never operations. v0.19 2026-09-12, P7a the interpreter-hosting layer CLOSED both hands (board :3989 inner / :3990 outer): CPython runs through the layer in the pinned guest, sealed and hash-checked, memory the declared twelfth act; ATTESTED 57; P3 re-mints on it. v0.18 2026-09-12, P4's two raises ruled (board :3985, the hand-ups carried late): the wake checklist's wiring into the composed kernel is P3's, at our own kernel's boot after the two-body check, not a P4 gap; a genesis row unrecordable because the record cannot fold is inherent and the refusal holds, the second body of the genesis pair witnessing it over its own record file. v0.17 2026-09-12, the vault completion closed both hands (board :3977); P2 complete with no exception. v0.16 2026-09-12, P7a countersigned (board :3976, c680ca26): no spike first, no founding; memory a declared growth of the act set. v0.15 2026-09-12, the kernel generated from the record (B11, L16) on the owner's word, board :3973. v0.14 2026-09-12, Q1 resolved: our own kernel (board :3972); L9 replaced; P3 and P7a rows updated; §8 line. v0.13 2026-09-12, P4 and P6 CLOSED both hands (board :3970, :3971); §8 lines. v0.12 2026-09-12, the vault on the owner's own word: freeze set aside for the rewiring, put back on after (Q7). v0.11 2026-09-11, P6 countersigned (board :3955, 60e8a111): the pairing code not a constitution rule; the genesis part defined; two by-name precisions. v0.10 2026-09-11, P4 countersigned (board :3950, 1ec20adb): the gating is code not a constitution rule; two by-name precisions. v0.9 2026-09-11, P3s closed and absorbed (board :3946): P7 split into P7a (before P3) and P7b; P4 and P6 performer-independent, run now; L9 amended with the chip fact; Q1 asked with facts, Q2 the architect's position; §8 line. v0.8 2026-09-11, P3 refused for a spike (board :3939); row P3s the candidate core census; §8 line. v0.7 2026-09-11, P2 CLOSED both hands (board :3934) with the vault completion delta owed; L15 the count-pin home; §8 line. v0.6 2026-09-11, the vault re-ruled at the definitive level on the owner's words (board :3930): it routes through the seam, secrecy a property of the act set, the byte-freeze retired as a guard; Q7 resolved, L14 and I5 precised. v0.5 2026-09-11, the P2 pin conflict ruled (board :3927): I5 precised (property not location), L14, Q7 the vault word for the owner. v0.4 2026-09-11, P2 countersigned (board :3922, 530e2c4b) with two by-name precisions. v0.3 2026-09-11, P1 closed and absorbed (board :3919): Q4 resolved, B10 and P9 added, L12 and L13, P2 retiered YELLOW, §8 first line. v0.2 2026-09-11, ACTIVE at DIGEST-C6a; nothing else moved. v0.1 2026-09-10, first cut at the architect's hand, beside C6a's last rows, from design/47 §1–§9, the C7 dialogue (D07.8–D07.12), the owner's three criteria (board :3860) and the proceed-auto ruling (board :3877). Written on a WORKING ASSUMPTION named in §5 [L9]; sizes are estimates, Low confidence, stated in §7.
    LAST-AS-BUILT-SYNC: none yet
    THE CORE (the owner's word, 2026-09-12, board :3972): OUR OWN KERNEL, written from a blank page, read whole and signed by us, performing exactly the eleven kinds of work the seam declares. The earlier working assumption of a sealed verified kernel was the architect's and is struck. The interpreter question (Q2) stays the owner's at P5's step.

## 1. HEADER — what this is, in one paragraph

Today the estate is an operating system's architecture running hosted: the record is the truth, every act crosses the gate, every state is a computed view, and Linux performs the disk, the one network connection and the clock beneath a declared seam. C7 replaces the performer beneath that seam with our core and changes nothing above it. It adds the three bodies that cannot be borrowed: the glue to the sealed core, the genesis checker that pairs with a second body at every waking, and the translator that lets borrowed drivers run as enclosed workers. It proves the swap by the same acts under two performers, one seam. Estimate — eight plans, three of them one-way-door adjacent and held at their irreversible step; weeks, not days. Confidence Low on size: the seam census (P1) is the first act because the size is not derivable before it.

## 2. OUTCOME — in the owner's language

The machine boots into our core in the guest with no Linux beneath it, checks its own body, its record chain, its constitution and its keys before its first act, and records that two separate witnesses agreed it woke sane. The same acts the record performs today perform on our core, and the numbers are read side by side. A borrowed driver runs unmodified and every device effect is a row. After this the estate can say "an operating system" plainly, with one cap: narrow by design, never general-purpose.

## 3. TARGET BEHAVIORS — numbered, testable

- **B1 THE MACHINE BOOTS INTO OUR CORE.** In the guest, the record, gate and views come up on our core with no Linux beneath the seam; the disk, the network connection and the clock are performed by our core. Check: the whole ledger runs green on our core in the guest; a probe for any Linux-only path beneath the seam finds none. [RE-WORDED :4050 on the P3b-5s measurement: 'the whole ledger green on our core' was an assumed list; the measured acceptance is the runnable set (146 of 192 at the measurement) green on our body and the not-runnable set declared by facility as a record, never served by adding fork/exec, a server socket or a kernel-event watch — L1.]
- **B2 NOTHING ABOVE THE SEAM CHANGES.** Every module above the seam is byte-identical to its C6a close except the seam adapter's registration. Check: a byte census over src/kernel against the C6a close hash; the campaign-1 to -6 pins unmoved.
- **B3 THE SEAM IS A CONTRACT THE RECORD CAN CHECK.** The acts that cross the seam are enumerated as a closed set; the estate above the seam runs green against a STUB performer that performs nothing but the declared acts. Check: the stub run is green; a planted undeclared crossing reds the census.
- **B4 THE WAKE CHECKLIST RUNS BEFORE THE FIRST ACT, AND ITS VERDICTS ARE ROWS.** Body, chain end to end, constitution read back exact, views folded fresh, keys valid and the account chain intact; only then the first act. Check: the genesis row carries the six verdicts; a planted failure at any step refuses the first act.
- **B5 THE GENESIS ROW IS A MUTUAL RECEIPT.** Two witness bodies that share no write path verify each other's genesis and compare copies; the row records both verdicts. Check: a planted disagreement between the bodies refuses the wake; one body alone cannot write the row.
- **B6 THE SEALED ARTIFACTS ARE PINNED IN THE RECORD.** The core and the interpreter are consumed as sealed released artifacts: each is named by hash in a record row and re-attested at every boot as part of the body check. Check: a planted byte change in either artifact reds the boot attestation.
- **B7 A BORROWED DRIVER RUNS AS AN ENCLOSED WORKER.** The translator performs only the common driver services; a driver from the narrow device list runs unmodified; every device effect and every device discovery is a row; a refusal reaches the driver as its own native failure answer. Check: the enclosed run is green for the device list; a planted effect outside the declared services is refused and recorded.
- **B8 NO SILENT LINUX.** Every place above the seam that assumes Linux is labelled or gone. Check: the stub-performer run of B3 is the mechanical form; a label census over src/ names every remaining assumption.
- **B9 THE NUMBERS ARE THE SAME ACTS UNDER TWO PERFORMERS.** The four numbers of design/50 are read under Linux and under our core, the same seam, the same acts, and published side by side. Check: both runs exist in evidence with identical act sets.
- **B10 THE OBSERVE WINDOWS UNDER OUR CORE.** The five observe windows read host facilities our core does not present; after the swap each window is either re-sourced from the core or declared ABSENT as a record, never silently empty. Check: a window census under our core names every window's state; a planted silent-empty window reds. (added :3919, from P1's findings)
- **B11 THE KERNEL IS GENERATED FROM THE RECORD.** The kernel's definition is rows in the record (the eleven kinds of work, each with its contract); its code is derived from those rows at build; at every waking the running kernel is checked against the rows by hash and by its declared work, or the first act is refused. Check: delete the built kernel, regenerate it from the record, the same bytes come back (the owner's word: "as long as that delete and regen work, like the demo, it is fine"); a planted kernel whose work differs from the rows refuses the first act. (added :3973)

## 4. INVARIANTS — what must never break

- **I1 One pen per record; the gate the only path for an effect.** Checked by the standing gate tests and the seam census (B3).
- **I2 Nothing above the seam names the performer.** Checked by the stub run (B3) and the label census (B8).
- **I3 No opaque code inside the signed base.** The signed base is the core glue and the genesis checker, each read whole; borrowed code lives only as enclosed workers. Checked by the attestation member list and the enclosure census (B7).
- **I4 The constitution lives once, in the record.** No copy on the chip body, no copy in the core. Checked by the genesis-body byte census (B5).
- **I5 Every campaign-1 to -6 pin unmoved.** Checked by the whole ledger per module. PRECISED :3927: unmoved means the PROPERTY holds and the guard stays able to fail; a guard follows the code it guards when that code moves behind the seam, keeping its planted failure and gaining the absence assertion on the old file; a byte-freeze is a guard of a property and is replaced by a guard of the property itself, never re-pinned (:3930).
- **I6 Revertability.** Every C7 row is code with tests behind a fence, git-revertible; no key bound, no ceremony run, no hardware bought, no public release, on the owner's word of 2026-09-10. Checked at every countersign by name.
- **I7 The witness classes are kept apart.** A body that hosts the gate is code-witnessed; everything else is act-witnessed. Checked by the attestation member list against the enclosure list.

## 5. SETTLED LAW — rulings register

- [L1] C7 is a swap, not a rewrite: the performer beneath the seam changes; nothing above it does; portable is not general-purpose. source: owner, D07.8, 2026-09-05
- [L2] Two witness classes: witness-the-code for whatever hosts the gate; witness-the-acts for every entity; the correct form is the composite, never opaque code in the signed base. source: owner-archi, D07.8
- [L3] Genesis is a standing guardian family: boot, recovery, checkpoint and attestation are one mechanism at four sizes; the wake checklist is its mechanical meaning. source: owner, D07.9
- [L4] The genesis row is a mutual receipt of two bodies that share no write path; the chip body carries the genesis part only, never the constitution. source: owner, D07.10, D07.11
- [L5] Air and water: the translator supplies the common driver services only; bus layer and drivers come in unmodified as enclosed workers; record management fully retained and fully invisible to borrowed code. source: owner, D07.11
- [L6] The no-silent-Linux rider: a Linux assumption may stand but never silently; a label rule, no rewrites, no deadline. source: owner, D07.12
- [L7] The three criteria, in order: definitive-level accuracy, universal derivability, max architecture-first outcome. source: owner, 2026-09-10, board :3860
- [L8] Proceed automatically at C6a's end; low-risk, easily revertable decisions are the seats' own; one-way doors stay the owner's. source: owner, 2026-09-10, board :3877
- [L9] OUR OWN KERNEL: written from a blank page, read whole and signed, performing exactly the seam's eleven act kinds (one disk, one connection, one clock), portable not general-purpose. source: owner, 2026-09-12, board :3972 ("I always thought we are building our own kernel"); the sealed-kernel working assumption that stood here was the architect's and is struck.
- [L10] The local slice a session carries (C6a VT-7) is consumed by C7 beneath the seam, never re-built. source: archi, board :3865
- [L11] Any kernel touch runs only in the guest, pinned and re-verified at every dispatch. source: EP-00 rule 9, charter §A21
- [L12] A spike's findings file is materialized by the manager from the builder's returned text after re-gating a sample of its counts, or the plan names the manager as the writer; a harness guardrail refuses a subagent that write. source: mgr finding at P1, archi ruled, board :3919
- [L13] Two seams, not one: the EFFECT seam is a closed act set the stub contracts; the OBSERVE seam is a separate read-only contract (observe/seam.py) left untouched by P2. source: archi on P1's findings, board :3919
- [L14] A pin guards a property, not a location: when routed code moves behind the seam its guard moves with it, no assertion deleted or weakened, the old file asserted free of the literal. A byte-freeze is a guard of a property, not a law; it is retired when a guard of the property itself exists (the vault: no reveal path, no read act in the seam) — :3930. source: archi, board :3927
- [L15] An absolute attested-member count pin lives only in the attestation tests; any other unit pins its own property (its fence added no engine module), never the global count. source: archi on mgr's note, board :3934
- [L17] MEASURE BEFORE AN UNKNOWN ROW; INSTRUMENT BEFORE A HARDWARE ROW. A body slice whose need cannot be named from what already exists (what the interpreter asks, what the ledger asks) is preceded by a throwaway measurement that names it (P3b-4s, P3b-5s), never cut from an assumed list; a slice whose shape the hardware dictates (memory tables, interrupts, a disk driver) is not delayed by a measurement but is authored with the instrument attached from the first line (the nested qemu's debug stub, the serial line), so a fault is read, never guessed. A slice with a nameable fence and a runnable acceptance builds straight, because a slice is cheap to redo and a stalled ladder is not. source: owner ask 2026-09-12 (orbit before landing), archi ruling board :4023
- [L18] THE BODY'S MODE, AND THE SEAM AT THE METAL. The body runs in the machine's own long mode (x86_64): the mode of the family it targets, of the pinned guest, and of every borrowed worker it hosts. A borrowed worker is supplied UNMODIFIED (L5 applied to the interpreter: the body fits the worker; the worker is never rebuilt to fit the body), runs UNPRIVILEGED (user privilege), and every request it makes crosses into the body by the machine's own call crossing — that crossing IS the seam at the metal, so enclosure is a hardware fact and every request is a row by construction (I3, I7) — PRECISED :4085: a row of the BODY'S OWN append-only crossing trail (served and refused alike, the refusal carrying the native answer returned), never an unsigned row appended into the estate's chain, which the body cannot sign; the trail's digest becomes a record row through the gate above as the worker's act-witness (P3b-4c/P3b-5), and the estate's act rows stay the gate's. The body answers exactly the requests measured (P3b-4s: forty-nine, in the measured x86_64 shapes): the twelve acts' share by the acts, the floor as the memory and concurrency acts realized, the designed refusal, the stubs; nothing beyond the measured set (L1). The mode is code behind a fence, git-revertible (I6), the seats' own under L8. source: archi, 2026-09-12, board :4078, on the P3b-4 stop (:4076); the fork the sizing spike named at Q-A and no seat ruled. PRECISED 2026-09-13 (board :4133, :4134, :4135, the pre-flight measurement): the measured set is FIFTY — the P3b-4s census missed a thread's own end (SYS_exit, distinct from exit_group; refused, a join hangs); a request beyond the set is refused (readlinkat by a static program — refused and tolerated, measured); the sealed image is a read-only filesystem the body serves by path AND by listing, because the interpreter's import machinery lists a directory once and fails outright when the listing is refused — the designed refusal is any path outside the image; the body enables SSE and never the extended state (CR4.OSXSAVE clear), so the C library selects SSE2 variants and the per-thread save is fxsave's 512 bytes. PRECISED :4247: THE SERVED SET IS PER ENCLOSURE -- the body serves each hosted worker exactly its own measured set, declared as data per enclosure; a shape in one worker's set is refused by name to every other; the interpreter's fifty stands untouched when a second worker (the borrowed TCP) brings its own set with the two frame shapes and a wait; every crossing of every set is a trail row. [PRECISED :4295, 2026-09-16: a worker's measured set is its WHOLE-PROCESS set, the C runtime's pre-main floor included, never the after-main calls alone; where a second enclosure's floor shapes are performers the body already has for the first, they are declared as data for the second, not re-built; a shape in neither declared set stays refused by name.]
- [L19] THE PROVER IS NOT OURS TO WRITE. A slice that claims to serve a borrowed program's needs is proven by a program NOT written to fit the body: one compiled by the ordinary toolchain against the real C library, whose own start-up, memory, thread-local base, threads and waits depend on the machinery being real — a static one first, a dynamically linked one next, the borrowed interpreter last. A hand-written worker that asks and accepts any answer in the measured shape is a check that cannot fail, and a body that answers it is a model, not a performer. The measurement (P3b-4s) names WHAT is asked; only the prover decides whether the answer is real; a planted failure must make the prover fail, never a flag. source: archi, 2026-09-13, board :4104, on the P3b-4c stop (:4102); the P3b-4b close (:4100) was over a purpose-model at the architect's hand.
- [L20] THE HOSTED PROGRAM OWNS THE STANDARD LOW ADDRESS RANGE. An ordinary program is linked to load at the platform's canonical base (0x400000 on x86_64) and the interpreter is such a program (ET_EXEC at 0x400000, driven 2026-09-13); the body therefore never claims that range for itself: the body's own image sits beneath the program's base (at 1 MiB), the body reaches physical memory through its OWN direct map above the user range (the higher half), and everything from the program's base up to the top of the user range is the hosted program's — its text, break, maps, stacks and the loader's placements. No prover and no borrowed program is ever re-linked or re-based to fit the body (L19, L5); the body is laid out to fit them. source: archi, 2026-09-13, board :4122, on the 4f-i stop (:4120); the layout was a P3b-1/P3b-4L implementation choice the architect never questioned at either countersign.
- [L16] The kernel is generated from the record and verified against it at every waking; it is never a truth beside the record; its acceptance is delete-and-regenerate. source: owner, 2026-09-12, board :3973
- [L21] NO GENERAL WRITABLE FILESYSTEM ON THE BODY; WORLDS ARE NAMESPACES. The body's writable surface is exactly the seam's declared write acts (append to a record, write-once a file by temp-and-swap, declare a directory, remove, the writer's lock) under per-world namespaces on the record disk; a general writable filesystem is Linux's derivative shape and is refused by L9. A test world on the body is such a namespace, served by the existing acts; a write class the seam does not declare refuses by name until a measurement shows the PROGRAM (never the harness) needs it. No served shape may answer success without performing the act: a stub that answers 0 and does nothing is the prover-shape L19 forbids, and is either made real or made a refusal. The on-body pass set of the ledger is MEASURED by write class, never chosen to fit. source: archi on mgr's :4234 finding, board :4235, 2026-09-14 PRECISED :4244: the record disk's FORMAT may grow to carry namespaces (paths as names, many record entries, a directory region and image sized by measurement, removal reclaiming space) while its properties (append-only, write-once, one writer) and their pins stay; a namespace held in body memory is refused as a second truth beside the disk. PRECISED :4248: (a) the writer's lock as the program issues it (open, the held lock, truncate to nothing, the holder's mark) is served as the one-writer act ON THE LOCK NAME CLASS ONLY; truncation elsewhere refuses by name. (b) Write-once is by name AND content: identical bytes to a present name answer success with nothing performed, different bytes refuse by name. (c) Served shapes are not act kinds: the count of kinds stays TWELVE while each enclosure carries its own served shapes (L18).

## 6. MUST-NOT-TOUCH

    src/kernel/**            except the seam adapter's registration point, named per plan
    src/founding/founding-pack.json   except by a founding row under the founding-mover discipline
    archive/**, tests/archive/**, reference/**
    the outside assessment harness (never read, never run)
    the campaign-1 to -6 pins (never loosened)
    any physical device, any chip body, any key ceremony, any public release (owner's one-way doors)

## 7. PLAN LIST — the campaign's shape

    P1 | THE SEAM CENSUS (spike)          | sizes B3, B7, B8 — every act that crosses the seam today, the real call list of the narrow device list, every Linux assumption above the seam | SPIKE (findings only, code thrown away) | 1 | CLOSED :3919 (inner :3918; the effect seam a closed set of about eleven act kinds at about sixty sites; L12, L13; B10/P9 added; §8 line of 2026-09-11) [row re-stamped 2026-09-16 at the architect's hand: it read PENDING while §8 held the close]
    P2 | THE SEAM AS A CONTRACT           | B3, B8 (the closed act set; the stub performer; the label census) | YELLOW (interface — it threads the record pen, the blob store, the vault and the commit window through the declared act set; the host-PID single-writer lock is the least portable site; retiered :3919) | 2 | CLOSED :3934 (inner :3933; the interface attested 55; sixty sites routed; the store guards re-pointed and strengthened; the vault completion CLOSED :3977: rewired and byte-frozen again at the new contents, no exception)
    P3s | THE CANDIDATE CORE CENSUS (spike)  | sizes P3, P5, P7's ordering — which sealed kernels boot in the pinned guest; by what path the sealed interpreter runs above each; the glue's size; what each choice forecloses (the owner's Q1/Q2 material as facts) | SPIKE (findings only; the manager materializes the file, L12) | 2b | CLOSED :3946 (inner :3945; the ordering finding landed: the interpreter-hosting layer precedes the core; seL4 verified on ARM, the guest x86_64)
    P3 | THE KERNEL FROM THE RECORD        | B11 (the twelve kinds of work as rows of law, genesis-seeded; the kernel's act set derived from them at build; delete-and-regenerate the acceptance; the running kernel checked against the rows at every waking after the two-body check, or the first act refused) | YELLOW, FOUNDING (1.54.0 -> 1.55.0) | 3 | CLOSED :3997 (inner :3996; countersigned :3993, sha 3a9ebbc9; founding 1.55.0; scope SPLIT ruled — B1 moved to P3b)
    P3b-s | THE KERNEL'S BODY, SIZED     | a spike: the thinnest body generated from the rows that boots in the pinned guest and writes one line on the serial line; the size of the bottom part (start-up, memory, interrupts, timer, serial, the guest's disk and network) as findings, code thrown away | SPIKE | 3b-s | CLOSED :4005 (inner :4004; the probe green — ~30 lines, the rows' digest on the serial line; the full body campaign-scale, cut into six slices below)
    P3b-1 | THE BODY: MEMORY                | B1 slice 1 (the twelfth act on the metal — physical/virtual memory bring-up in the body; the rows' digest still on the serial line; the single-writer-lock's meaning at the metal stated on its row) | YELLOW | 3b-1 | CLOSED :4015 (inner :4013; countersigned :4009; ATTESTED 68)
    P3b-2 | THE BODY: THE DISK AND THE RECORD ACTS | B1 slice 2 (the nested guest's disk; record-pen, atomic-write-once, remove, body-read, founding-pack-read performed by the body; a record appended and read back) | YELLOW | 3b-2 | CLOSED :4031 (inner :4027; countersigned :4019; ATTESTED 73; evidence dir owed)
    P3b-3 | THE BODY: CLOCK, INTERRUPTS AND ENTROPY | B1 slice 3 (recording-clock, commit-window and entropy on the metal — a seeded real source of chance, never baked into the body; an interrupt table, a controller and a timer; a cooperative body suffices here) | YELLOW | 3b-3 | CLOSED :4072 (inner :4071; countersigned :4058 on 9ee99f6a; ATTESTED 79; evidence dir owed)
    P3b-4s | THE INTERPRETER'S NEEDS, MEASURED | a spike, runnable now on the pinned guest with no body: the sealed CPython through the P7a layer under a system-call trace; every distinct call of the Linux kernel with its argument shapes (start-up, import, memory growth, threads, files, time, entropy); the calls the body will NOT serve named | SPIKE | 3b-4s | CLOSED :4043 (inner :4041; 49 calls, twelve acts unwidened; the list P3b-4 is cut from)
    P3b-5s | THE LEDGER'S NEEDS, MEASURED | a spike, runnable now on the pinned guest with no body: the whole per-module ledger under the same trace; every distinct call beyond the interpreter's own, by test module (subprocess, temp dirs, sockets, signals, timers); the tests that cannot run on the body declared before they red | SPIKE | 3b-5s | CLOSED :4050 (inner :4048; 146 runnable / 46 declared out by facility; the list P3b-5 is cut from)
    P3b-4 | THE BODY: THE INTERPRETER RE-HOMED | B1 slice 4 — RE-CUT :4078 into the four rows below on the STOP :4076 (the plan as authored unrealizable: a 32-bit body cannot host the 64-bit sealed interpreter); the :4062 countersign STRUCK; the P3b-4s measurement stands as the specification | — | 3b-4 | RE-CUT :4078 (was COUNTERSIGNED :4062 sha 176be86c; STOPPED :4076; OWED :4077)
    P3b-4L | THE BODY IN LONG MODE           | B1 (the three closed slices re-based to the machine's long mode, L18: the start-up trampoline, four-level paging, the 64-bit interrupt table and stubs, pointer widths; every P3b-1/2/3 self-check green again with its meaning unchanged and its planted failure still failing; the twelve rows' digest still on the serial line; the build byte-reproducible; the attested set moves by name) | YELLOW | 3b-4L | CLOSED :4094 (inner :4093; countersigned :4082; ATTESTED 79, edit-in-place; body.elf 17c03447 / body.img 26468a63)
    P3b-4a | THE WORKER'S ENCLOSURE         | B1 + I3/I7 (an unprivileged worker loaded by the body's own loader from a sealed image; the call crossing into the body and back; a small hand-written test worker of OURS, attested by name, makes a handful of requests and reads the body's answers; a request outside the served set is refused as the worker's own native failure answer and is a row) | YELLOW | 3b-4a | CLOSED :4097 (inner :4096; countersigned :4085; ATTESTED 85, six body files added by name; body.elf d699fd51 / body.img f343e4a5)
    P3b-4b | THE FORTY-NINE SERVED          | B1 (the measured requests in their measured shapes: the twelve acts' share by the acts; the floor as the memory and concurrency acts realized on the metal — memory maps, threads, the wait, thread-local storage, what the loader asks; the designed refusal — module bytes from the sealed image, no filesystem walk; the stubs degrading; entropy at bring-up beneath the act's layer; memory private, no fork, Q11; each proven with the test worker request by request; a HOW-split is mgr's) | YELLOW | 3b-4b | CLOSED :4100 over a PURPOSE-MODEL, RE-OPENED :4104 (the classification by purpose stays as the router; the canned answers behind it are replaced by P3b-4f/P3b-4d; the :4100 close stands as a record of what was built)
    P3b-4m | THE ADDRESS SPACE LAID OUT FOR THE PROGRAM | B1 (L20 realized: the body's image stays at 1 MiB beneath the canonical program base; the body's access to physical memory — frames, page tables, disk buffers, the heap's frames — moves to a direct map above the user range; the supervisor identity map over the low gigabyte is withdrawn from the program's range; user pages mappable from the canonical base up; the P3b-4a worker re-linked to the canonical base with its tests unchanged in meaning; every P3b-1/2/3/4L/4a/4b self-check green with meaning unchanged and every plant live; the digest still on serial; reproducible) | YELLOW | 3b-4m | CLOSED :4144 (inner :4143; countersigned :4126; ATTESTED 87 unchanged, edit-in-place; the direct map a fixed 1 GiB cap with a check that fails visibly above it — the report-sized map a residue, not blocking) — was COUNTERSIGNED :4126 (sha 662d3e0c; dispatchable at mgr's call, before 4f-i; two by-name precisions)
    P3b-4f | THE FLOOR REAL                 | B1 (the floor served by REAL machinery: the loader's stack and auxiliary vector for a static program; memory maps and the break as a real address-space manager; the thread-local base installed in the machine's own register per thread; threads as real contexts scheduled and preempted by the body's timer; the wait a real wait queue with timeouts and wake; exit clearing the child id — routed by P3b-4b's classification, the shapes unchanged; PROVEN by a statically linked C program built by the guest's ordinary toolchain with the real C library's start-up and threads (L19): its start-up runs, two threads under a lock reach the exact count, a timed wait times out, allocation works; planted: no base installed faults the program, a fake thread id fails the join, a wait that returns at once loses the count) | YELLOW | 3b-4f | SPLIT by mgr :4113/:4114 (the :4107 countersign superseded by the split; its three precisions carry into both rungs): 4f-i THE STATIC SINGLE-THREAD FLOOR (stack + auxiliary vector, the address-space manager, the main thread's base, the machine's SSE state enabled; prover a static single-threaded C program) 4f-i CLOSED :4159 (inner :4158; the static prover independent at both hands; six-key auxv; SSE on, OSXSAVE never) — was COUNTERSIGNED :4116 (sha d345bb9b) — STOPPED :4120 on the layout collision, RULED :4122: A1 stands verbatim (an ordinary gcc -static program at its own base, no base flag), dispatched after P3b-4m's inner close · 4f-ii THE CONCURRENCY FLOOR (threads, the preemptive scheduler on the body's timer switching each thread's base and arithmetic state outside any act, the real wait queue, exit clearing the child id; the frozen 4f A1 verbatim — the threaded prover) 4f-ii CLOSED :4171 (inner :4169; the threaded prover independent; SYS_exit served; per-thread fxsave/fxrstor outside acts; four latent crossing defects found by the real program and fixed) — was RE-MINT COUNTERSIGNED :4140 (sha f94a0e50; supersedes :4119 (sha fead67d2; after 4f-i's inner close)
    P3b-4d | THE DYNAMIC LOADER AND THE SEALED IMAGE | B1 + B6 (the sealed image as a read-only filesystem of named files the body serves by path and offset — the dynamic linker, the C library and its companions, later the interpreter's module set as one archive and gov-os's source blob; the loader honouring a program's interpreter entry and handing over; the writable record filesystem of P3b-2 mounted beside it for the record's paths; the image digest-checked before load; PROVEN by a dynamically linked C program with threads from the guest's own C library (L19); planted: a byte change in the image refused, a path outside the image refused, the linker's own probes answered as measured) | YELLOW | 3b-4d | 4d CLOSED :4192 (inner :4190; the real ld.so 2.39 from the sealed digest-checked image, seven-key auxv, the image listable; the dynamic prover independent) — was RE-MINT COUNTERSIGNED :4141 (sha 40540211; supersedes :4110 (sha 3aaa1fbb; after P3b-4f's inner close; three by-name precisions)
    P3b-4c | THE INTERPRETER RUNS ON THE BODY | B1 slice 4 as first cut: the sealed image (the interpreter, its C library and the loader-visible pieces, ONE hash, ONE row, :4062 precision a standing) loaded by the body in the nested guest; print(2+2)=4 and a thread; P7a's layer re-homed onto the body's acts, not rewritten; the digest still on serial; the drifted anchor re-sealed and re-pinned; Q2 untouched | YELLOW | 3b-4c | 4c THE CRUX CLOSED :4218 for A1/A2/A5/A6 (inner :4206; the real unmodified CPython 3.12.3 runs on the body from the sealed image through the real ld.so); A3/A4 RE-CUT to the row below — was RE-MINT COUNTERSIGNED :4142 (sha c9b307c1; supersedes :4112 (sha c2f37fd5; the :4091 countersign struck at :4104; after P3b-4d's inner close; two by-name precisions)
    P3b-4g | THE GATE ON THE BODY | B1 (gov-os's own gate, running inside the body-hosted interpreter, writes and signs a record row from a distinct on-disk witness — key material from the record disk, the row read back by the body-read act, the trail's digest the act-witness row of L18; proven OFF the body by the row verifying under the bound public half; plants: a tampered trail digest refused, a row written without the gate refused by the chain) | YELLOW | 3b-4g | CLOSED at both hands (inner :4231, outer :4232) on the re-mint countersign :4227 (sha 15f9b9c3; :4225 struck); state proved MODELLED and named
    P3b-5 | THE BODY: THE LEDGER GREEN ON IT | B1 slice 5 (the measured runnable set of gov-os's ledger green on our own body, skips reported by name; the not-runnable modules declared by facility as a record — subprocess, the kernel-event watch, a server socket — never a silent red, never served by widening the body; the content axis settled per module; the wake checklist and the genesis pair at its boot, the first actor act refused on any failure) | YELLOW (the irreversible step — standing it as the production performer — held, the owner's; the public page's one word changes here, Q5) | 3b-5 | STOPPED :4234 on a false premise (the census counted syscall facility, not path writability); RULED :4235 -> re-authored as the ladder below; :4065 superseded when 5b freezes
    P3b-5s2 | THE LEDGER'S WRITES, BY CLASS (spike) | measures, on the guest with argument shapes, every write the 146 runnable modules make (open flags by path class, mkdir, rename, unlink, truncate, overwrite, mmap-write) and classifies each module by write class -> the measured on-body pass set; the excluded named with the class and its owner (program or harness); the slowdown per :4153 e | SPIKE | 3b-5s2 | FINDINGS ON DISK :4248 at the architect's hand (the SECOND event on that physical line -- the line holds two glued events and the fold shows only the first, ruled C7-MAINT-BOARD-GLUED-LINES :4289; a spike ends on its findings) (findings on disk: 81 pass as the code stands, 136 with the lock idiom served; sizing 3,749 worlds / per-module max 1,132 paths / 92-byte names; bytes a cap for 5a's pre-flight)
    P3b-5a | THE BODY: WORLDS AS NAMESPACES | B1 (under /rec/ the body serves mkdir as a real namespace declaration, create-on-absent as write-once, append as the record pen, unlink as removal, same-directory rename as the write-once swap, listing and stat; the whole gov-os tree as content in one archive on the record disk with a finder; the disk format grown to BODYFS02 -- paths as names, many records, sized by the 5s2 measurement, the P3b-2 pins re-pointed with the code; every other write refuses by name; the two no-op stubs made real or refusing) | YELLOW | 3b-5a | CLOSED at both hands (mgr :4277/:4279/:4282 in three HOW slices; outer :4283) on the countersign :4257
    P3b-5a-iv | THE RECORD PEN AT CALLER-NAMED NAMES | B1 (under /rec/ the record pen's first stroke creates the record: O_CREAT|O_APPEND on an absent name creates and appends, on a present name appends; O_TRUNC on a present record still refused; the real store's open_append the prover) | YELLOW | 3b-5a-iv | AUTHORED :4326, COUNTERSIGNED :4327 (sha 56de4c9b); DISPATCHED :4331; CLOSED at both hands (inner :4335, outer :4337); 5b resumes on it
    P3b-5a-v | THE SEAM'S PERFORMERS SERVED EXACTLY | B1 (the body performs the exact system-call shapes of host_seam.py's real class: write-once by temp-then-swap with O_TRUNC on an absent name a create; the one-writer act as O_CREAT|O_RDWR on the marker plus fcntl F_SETLK held, a second process refused and a second same-process claim granted as POSIX grants it; read-after-write on a caller-named record at any size; O_TRUNC on a present name still refused) | YELLOW | 3b-5a-v | AUTHORED :4345, COUNTERSIGNED :4346 (sha c28c8cfc; the second-claimant refusal corrected to POSIX per-process semantics); DISPATCHED :4350; CLOSED inner :4352 / outer :4354 (the three performers served; the second same-process lock claim built as a refusal against the countersign, ruled to C7-MAINT-LOCK-POSIX-PROCESS-CLAIM :4355); 5b-ii resumed on the derived population :4353
    P3b-5a-vi | CEILINGS, CLOCK AND THE BLOB DIRECTORY | B1 (the body's open-file table is Linux's shape, its width what it reports at prlimit64 and at least the derived per-module maximum from the 5s2 traces; CLOCK_REALTIME answers the epoch time from the RTC read at boot plus the monotonic since boot, the timed-wait coupling of 4f-ii kept; a namespace declared one level under a world stats and lists as a directory, blob write-once under it served, nothing off /rec/; the four red modules test_ep44/24c/27b/26 green on it) | YELLOW | 3b-5a-vi | MINTED :4360 on mgr's raise :4358; AUTHORED by mtr on the glued line :4361 (sha 1001104b); COUNTERSIGNED :4362 with one precision (the timed-wait 'now' carries the same epoch offset as the deadline); mgr dispatches one builder at a time, serial on serve.c; 5b holds on it; DISPATCHED :4367; BUILT on three axes (fd table 48 to 1024, derived peak 203; the epoch clock with 4f-ii 17/17; the blob directory as FT_DIR, no on-disk format change) and STOPPED :4369 on a fourth axis (memory reclamation); A4 met by 5a-vi and 5a-vii together; CLOSED inner :4386 / outer :4389 (the three axes read in the code and the serials; the owed standing test undelivered, ruled to C7-MAINT-5A-VI-STANDING-TEST :4390)
    P3b-5a-vii | THE BODY RECLAIMS UNMAPPED FRAMES | B1 (the body's unmap act returns each frame it owns to the frame allocator, so map-and-unmap churn no longer exhausts the pool; free only a frame read from a PRESENT and USER page before the unmap; the dynamic loader's mapping never corrupted; test_ep26 green on the body) | YELLOW | 3b-5a-vii | MINTED by mtr on mgr's stop raise :4369 (option B); AUTHORED :4370 (sha 8fd11026); COUNTERSIGNED :4371 with three precisions; mgr dispatches one builder at a time, serial on serve.c; 5a-vi holds on it, 5b on 5a-vi; DISPATCHED :4372; BUILT the unmap reclaim (serve.c +58/-1, page-table guarded, two plants) and STOPPED :4373 on a second leak, the shrinking break (test_ep26 five MemoryError to four, pool drained), and on the 4d prover not booting on the original body either; RE-MINTED to EXTEND :4374 (sha 1774c16a); RE-COUNTERSIGNED :4377 with two precisions; closes at neither hand until the loader plant is boot-verified on the restored 4d prover; 5a-vi closes and 5b resumes on test_ep26 green; BUILT the shrink reclaim (test_ep26 green, pool sustained) and found the unmap plant cannot fail on test_ep26 (break-bound); RE-MINTED :4384 to home it on the 4d prover; REFUSED :4385 (that prover does not churn either); the unmap plant goes on a measured churn prover; RE-MINTED :4387 on that WHAT and COUNTERSIGNED :4391 (sha 5fdc1e6f); BUILT: the churn census (114 modules, none near the pool in-process) and a real-libc churn prover, the leak plant a real out-of-memory; the guard plant found inert on the 4d prover (:4397), RE-MINTED :4398 to the churn prover and COUNTERSIGNED :4400 (sha 6aab339f) with the plant ruled to the accounting form; BUILT tests-only and CLOSED inner :4404 / outer :4405: the page-check plant fires by real aliasing through a partial unmap of a two-page map (the architect's impossibility claim at :4400 wrong, disclosed); the 5a series done
    P3b-5b | THE BODY: THE LEDGER GREEN ON IT | B1 (the :4065 substance on a true premise: the MEASURED pass set green per module on the body, the excluded declared by class as a record, the wake checklist and genesis pair at the body's boot with planted refusals) | YELLOW | 3b-5b | COUNTERSIGNED on the re-mint :4294 (sha c522654d; first freeze refused :4288); DISPATCHED :4296; 5b-i stopped on the run mechanism :4300, RULED :4303 (walking skeleton first, the execution half inside 5b, five fidelity precisions); mgr re-dispatches 5b-i; 5b-i CLOSED green (:4308, :4312); 5b-ii STOPPED :4322 (none of 16 green: the caller-named create-append unserved), HELD on 5a-iv (:4324); A1's population re-derived by mtr against the body's served classes as built; resumed :4336, STOPPED :4341 (7 green, 11 red in three seam-act classes), HELD on 5a-v (:4343); the population to be derived mechanically from the traces against the seam performers' exact shapes; 5b-ii second pass STOPPED :4358 (8 of 12 green; 5a-v confirmed by real modules; four CLASS-B reds by three non-lock causes) -> RULED :4360 to P3b-5a-vi, HELD on it; third pass DISPATCHED :4407 and STOPPED :4408 (the 5a series confirmed by real modules; 29 green, 2 all-skip, 10 red: six content-axis-out :4410, four body shortfalls); the body-fix unit refused at the countersign :4412 (stat identity per file; tmp-world removal served; EBADF on an unknown descriptor)
    P3b-5c | THE HARNESS ON THE BODY | B1 (fixtures for modules excluded by a harness habit only: the temp root under the world namespace, the source pointer at the content, never a core widening) | GREEN | 3b-5c | PENDING (after 5b; only if 5s2 names harness-excluded modules)
    P3b-6 | THE BODY: THE ONE CONNECTION    | B1 slice 6 (network-socket on the metal, last) | YELLOW | 3b-6 | STOPPED :4246 (the borrowed TCP needs a frame crossing not in the served set); RULED :4247 -> split below; :4068 superseded when 6c freezes
    P3b-6a | THE BODY: THE NETWORK DEVICE, OURS | B1 (the virtio-net device driven by our own code, the virtio transport of design/57 §5; proven by the wire -- an address round trip answered by the guest's gateway, an outside party; the planted dead device reds; attested by name; no served shape added) | GREEN | 3b-6a | CLOSED at both hands (inner :4252, outer :4253) on the countersign :4250
    P3b-6b | THE BODY: THE BORROWED TCP ENCLOSED | B1 (a borrowed TCP stack unmodified as an enclosed worker with its own declared served set: its measured needs, send-frame, receive-frame, a wait; proven by the stack's own program opening the one guest-local connection; every frame crossing a trail row) | YELLOW | 3b-6b | STOPPED :4290 (a glibc static worker issues seven pre-main shapes beyond the eight) and RULED :4295: worker unmodified, the served set its whole-process measured set declared as data, the pre-main floor the performers 4f-i already gave the interpreter; the :4267 countersign struck; re-minted :4298/:4299 on mgr's whole-process pre-flight :4297 (20 distinct: 16 pre-main, 4 post-main, all performers the body has); COUNTERSIGNED :4302 (sha b66e77a7); DISPATCHED :4305/:4313; CLOSED at both hands (inner :4315, outer :4317; the premature revert :4314 disclosed and recovered)
    P3b-6c | THE BODY: THE SOCKET ACT WIRED | B1 (the interpreter's socket-family shapes relayed by the body to the enclosed worker; the planted-red matrix; attestation; the ledger's socket act on the body) | YELLOW | 3b-6c | CLOSED at both hands (inner :4599, outer :4602) — slices (i)/(ii) closed :4547/:4548 and :4556/:4557; slice (iii) countersigned :4561 (sha 32255e64), A2-A5 :4590, raises ruled :4591, grant-first sub-unit closed :4598/:4601; B1 MET — the body arc complete
    P4 | THE WAKE CHECKLIST               | B4 (the six verdicts as a genesis row before the first act; each step able to refuse) | YELLOW | 4 | CLOSED :3970 (inner :3960; six verdicts as a genesis row before the first act by an actor; no founding)
    P5 | THE SEALED INTERPRETER           | B6 (the gate's host consumed as a sealed artifact, pinned and attested at boot) | YELLOW (on L9; held at its irreversible step) | 5 | AUTHORED :4614, COUNTERSIGNED :4615 (routing confirmed: the body's own record-disk row, L18; two riders), then the :4618/:4619 raise RULED reading 2 (:4620): as frozen the core check ran only under the test rig (a real boot attaches no core module — driven at diskcheck.c:266/kmain.c:220/build.sh) — the buildable part owes the core self-attest in the REAL boot pipeline; CLOSED at both hands (inner :4624 at mgr's own re-gate, outer :4625) on the re-mint countersign :4622 (sha 09ed595b) — B6 MET: a real production boot stages the core (build.sh:521), writes both sealed-artifact rows whose hashes equal the released images measured independently outside, and reds on a planted byte in either, each way; Q2 (the named L2 exception + the released performer) remains the owner's, fenced and untaken
    P6 | THE GENESIS PAIR                 | B5 (two bodies, no shared write path — in the guest the second body is a separate image; the mutual receipt row; the physical chip body HELD for the owner) | RED at the physical step, YELLOW in the guest | 6 | CLOSED :3971 (inner :3962; two bodies, no shared write path, the mutual receipt; the physical body the owner's)
    P7a | THE INTERPRETER-HOSTING LAYER       | B7's first half (the common services the interpreter needs — the seam's eleven acts plus the runtime's own needs — presented beneath the seam; its spec is the closed act set) | YELLOW (the largest row; shape depends on Q1) | 3a | CLOSED :3990 (inner :3989; countersigned :3976 (c680ca26; memory a once-only growth of the act set; the libc surface above the acts; the stand-in beneath for this unit; the size stop real)
    P7b | THE DEVICE DRIVERS AS ENCLOSED WORKERS | B7's second half (the block, network and clock devices; discovery as rows; refusal in the driver's native language) | YELLOW | 7 | FORM RULED :4603, reading (A): block and clock stay ours-core (ata.c/bodyfs.c, clock.c — the :4246 ground); the enclosed-worker half of B7 cites the closed lwIP unit; P7b's content is discovery-as-rows over the three-device list (:3919) + refusal in the driver's native language + the device window re-sourced; CLOSED at both hands (inner :4610 at mgr's own full re-gate, outer :4611) on the countersign :4605 (sha 1fa82232) — B7's second half MET: three discovery rows with exported identities, device window re-sourced from them, drivers byte-unchanged sha-pinned, lwIP half cited; the fabricated-verifier-report disclosure recognized at :4611 (re-gate law held)
    P8 | THE NUMBERS AND THE REVIEW       | B9 (same acts, two performers; the four numbers side by side); the campaign review and digest | GREEN | 8 | NUMBERS HALF CLOSED (inner :4629 at mgr, archi close-read :4631): #1 696.8/181.8 rows-sec, #2 5.99us/204us, #3 Linux 1M (CPU 534.8s) vs body N=739 with the proof SIZE 347 B EQUAL BOTH, #4 floor bracketed 1 MiB red / 2 MiB append / 3 MiB clean; boundary estate gate driven (199 modules, 5 dispositioned reds, none C7's); REMAINING: the review at mtr (:4626), then DIGEST-C7 at archi
    P9 | THE OBSERVE WINDOWS UNDER OUR CORE  | B10 (each window re-sourced or declared absent as a record) | YELLOW | 9 | CLOSED at both hands (inner :4576, outer :4577); the file-window follow-on closed :4586/:4587

GREEN rows are pre-approved for dispatch on script-green the moment this paper goes ACTIVE; YELLOW rows are countersigned by the architect; a RED step is held for the owner's word by name.

## 8. AS-BUILT LOG — reality absorbed

- 2026-09-13 · P3b-4c (the interpreter runs on the body) STOPPED by the builder and the arc RE-CUT, board :4104 (stop :4102, owed :4103) · Nothing was built. On reading, the body's loader maps only a bare static program, and the forty-nine 'served' at the previous slice were answered in the right shape with nothing real behind several of them: no thread-local base installed, a made-up thread id, a wait that never waits, a fixed address for every map. A real interpreter cannot execute one instruction on that. The slice passed because its prover was a program of ours written to fit the body — a check that cannot fail — and the architect countersigned and closed it reading shape flags. Ruled: the stand-in option refused (a record must never hold an act nobody performed); the floor is rebuilt real and proven by programs not written to fit the body, a static C program first, a dynamically linked one next, the interpreter last; two rows cut before the interpreter's; the interpreter row's meaning unchanged. Estimate — three to five more slices; confidence Medium; the tail risk is the interpreter's own start-up on a homemade body. · §5 L19, §7, §9 Q13
- 2026-09-13 · P3b-4b (the forty-nine served) CLOSED, board :4100 (inner :4099) · Our own body now answers every one of the forty-nine requests the interpreter was measured to make, each in the shape it was measured in and sorted by what it is for rather than what it is called: the twelve kinds of work by the body's own act machinery, the program's coming-up and its threads by the memory and concurrency machinery, the scan of a live filesystem refused with the module bytes served from the sealed image instead, the probes answered so the program degrades rather than dies, chance drawn at bring-up, memory private with no fork, and the one network request refused in the program's own words until the connection slice; every crossing one row of the body's own trail, the body holding no key and writing nothing into the estate's chain. Two source files added by name (85 to 87). · §7
- 2026-09-13 · P3b-4a (the worker's enclosure) CLOSED, board :4097 (inner :4096) · Our own body now carries a program: a small test program of ours, loaded by the body's own loader from a sealed image the body checks byte for byte before loading, running with no privilege of its own (the processor's own privilege bits read back, a trespass on the body's memory faulting as the hardware dictates), reaching the body only by the machine's own call crossing; five requests crossed and were answered, one outside the served set refused in the program's own native shape, every crossing a row of the body's own append-only trail and nothing written into the estate's signed chain. Six new source files of ours attested by name (79 to 85). · §7
- 2026-09-12 · P3b-4L (the body in long mode) CLOSED, board :4094 (inner :4093) · Our own body now starts in the 32-bit form the loader hands it and steps itself into the computer's own 64-bit mode, reads that fact back from the processor's own register rather than from a compile-time constant, builds its own four-level page tables and abandons the start-up scaffold, takes interrupts through a 64-bit table, and runs every check the memory, disk and clock slices proved before with the same meaning and every planted failure still failing; the twelve rows' digest still on the serial line; two clean builds byte-identical. One mechanical fact absorbed: the nested VM's loader parses only 32-bit programs, so the reproducible 64-bit program is flattened by a deterministic step into the image the VM boots, both digests recorded. · §7
- 2026-09-12 · P3b-4 (the body: the interpreter re-homed) STOPPED by the builder and RE-CUT, board :4078 (stop :4076, owed :4077) · Nothing was built. The body had been carried forward in the 32-bit form the first thirty-line probe used, and the Python interpreter it must carry is the machine's 64-bit form, as is every one of the forty-nine requests we measured it making; a 32-bit body can neither load that interpreter nor answer those requests, so the builder stopped instead of forcing it. The spike that sized the body had named this exact fork and left it for a ruling; three slices were countersigned without one, at the architect's hand. Ruled: the body moves to the machine's own 64-bit mode and the three finished pieces are re-based onto it; the interpreter runs inside the body as an unprivileged, unmodified program whose every request crosses the machine's own boundary into the body, which answers exactly the forty-nine measured and refuses the rest; four rows replace the one. The one-way doors are untouched (no key, no ceremony, no hardware, no public word); the change is code, revertible. · §5 L18, §7, §9 Q12
- 2026-09-11 · P1 (the seam census, a spike) CLOSED, board :3919 (inner :3918) · The effect seam is a closed set of about eleven act kinds at about sixty sites in eight files, not yet behind one boundary; the observe seam is a separate read-only contract already in place; the governance core names no Linux facility. Rulings: the device list derives from the seam's three acts in the guest; the stub contracts the effect set only; P2 retiered YELLOW; B10/P9 added for the observe windows; L12 the findings-writer rider; L13 the two seams.
- 2026-09-11 · P2 (the seam as a contract) CLOSED, board :3934 (inner :3933) · Every act the record asks its host to perform now goes through one declared, attested place; a stub performer runs the estate above it; a census names any crossing that escapes; every site below carries its label. The guards that pinned where a call lived followed the call and gained an assertion that the old file no longer holds it. The vault's four sites are the one completion still owed, on the owner's words: it joins the seam, secrecy becomes a property of the act set (no read act exists), and the fingerprint tests retire.
- 2026-09-11 · P3 refused for a spike, board :3939 · The plan to stand our core beneath the seam in the guest named no candidate core and no path by which the interpreter that hosts the gate would run above it, so its first acceptance had no command to run. A census comes first: which sealed kernels boot in the pinned guest, how the interpreter runs on each, how big the glue is, and what each choice forecloses. That census is also the material for the owner's one-way door, stated as facts.
- 2026-09-11 · P3s (the candidate core census) CLOSED, board :3946 (inner :3945) · The finding that reorders the campaign: the gate runs on an interpreter, and no verified core presents what an interpreter needs, so the layer that supplies those common services comes before the core stands, and it is the same layer the seed called air and water. The verified kernel the working assumption pointed at carries its proof on ARM; the test machine is x86_64. The wake checklist and the genesis pair need no core and run now.
- 2026-09-12 · P3b-3 (the body: clock, interrupts and entropy) CLOSED, board :4072 (inner :4071) · Our own body now takes interrupts through its own table to a handler and back, runs a timer whose ticks advance, keeps the wall clock for recording and the steadily advancing clock for the commit window, and draws seeded chance with two draws differing, all in the nested VM with no Linux kernel beneath and the twelve rows' hash still on the serial line. A planted wrong interrupt entry made its own check fail; a silent hang was read from the debugger attached from the first line, as the standing rule requires. Two clean builds gave the same bytes, the seed being a runtime output never in the body. Six more files joined the one attested list by name (79). The serial transcript is owed to the evidence folder by the manager. No founding; the twelve acts unchanged. — §7 row P3b-3; §5 L17.
- 2026-09-12 · P3b-5s (the ledger's needs, measured) CLOSED, board :4050 (inner :4048) · Every test module of gov-os run on the pinned guest with its requests of the Linux kernel traced: of 192 modules, 146 use only what our narrow body serves and 46 cannot — 44 because they run an outside program (mostly git, checking the record's history on a full computer), one because it watches kernel events, one because it opens a server socket. So the fifth slice's acceptance is corrected from the assumed 'whole ledger' to the measured one: the runnable set green on our body, the rest declared by name and by the facility they need, never a silent failure and never a widening of the body to serve them. Thirteen of the runnable modules read files the body's record-only disk does not hold; the fifth slice settles each as content on the disk or as declared. The memory act's memory is nowhere relied on across a fork (Q11 confirmed). — §3 B1; §7 rows P3b-5s, P3b-5.
- 2026-09-12 · P3b-4s (the interpreter's needs, measured) CLOSED, board :4043 (inner :4041) · The sealed Python, run through the hosting layer on the pinned guest with every request of the Linux kernel traced, asks for forty-nine distinct things, twenty-seven at its leanest start. The twelve acts serve their share without any act being added. What remains splits three ways: a hard floor the body must supply beneath the layer (the loader, thread-local storage, thread creation in its exact shape), a designed refusal (Python's search of a filesystem for its modules — the modules travel inside the seal and the body serves their bytes from the sealed image, never a filesystem walk), and small stubs that degrade gracefully (terminal probes, seek probes, identity). Python asks for chance at its very start, before the layer exists, so the source of chance lives in the body itself, which is where the third slice put it. The memory act's Linux default is memory shareable across a fork; the act promises anonymous memory, not fork-sharing, and our body has no fork, so it serves the act private to the body (Q11). The guest's Python had drifted off the seal from the spikes' package install; the manager re-seals at the next slice. — §7 row P3b-4s; §9 Q11.
- 2026-09-12 · P3b-2 (the body: the disk and the record acts) CLOSED, board :4031 (inner :4027) · Our own body now has a disk: a disk driver, a small filesystem written for the record, and the five kinds of work that touch the record performed by the body itself in the nested VM with no Linux kernel beneath — a record appended and read back equal, a blob written once and durable, a removal, a walk of the body's own files, the founding pack read from the disk equal to the pack. The record on the body's disk can only grow at its tail: the filesystem has no overwrite path for it and a planted overwrite is refused; removal applies to blobs only. Record and blob survive a reboot; six planted faults each make their own check fail; two clean builds give the same bytes. Five more files joined the one attested list by name (73). The serial transcript is owed to the evidence folder by the manager. No founding; the twelve acts unchanged. — §7 row P3b-2.
- 2026-09-12 · P3b-1 (the body: memory) CLOSED, board :4015 (inner :4013) · The first merged piece of our kernel's body lives under src/body: eleven files (start-up assembly, the physical frame allocator, page tables and paging, a heap, the serial line, the generator that folds the twelve rows into the body's digest, a linker script and a build), every one of them in the one attested list by name beside gov-os's Python (68 members), the guard that catches an unlisted file extended to them. In a nested VM inside the pinned guest, with no Linux kernel beneath, the body boots, brings memory up, passes a self-check that two planted faults make fail, and prints the twelve rows' digest, byte-identical to the digest gov-os folds from the founding. Two clean builds gave the same bytes; reproducibility is reported here and binds at the fifth slice. No founding; the twelve acts unchanged. — §7 row P3b-1; §9 Q10.
- 2026-09-12 · P3b-s (the kernel's body, sized) CLOSED, board :4005 (inner :4004) · A body of about thirty lines generated from the twelve act-kind rows booted in a second VM nested inside the pinned guest, with no Linux kernel beneath it, and wrote one line on the serial line: the twelve rows' digest, byte-identical to the digest gov-os folds from the founding in main (driven at both hands). Start-up and the serial line need no memory manager, interrupts, timer, disk or network. The full body — the twelve acts on the metal, the interpreter layer re-homed above it, the ledger green on it, the wake checklist and genesis pair at its boot — is campaign-scale (thousands of lines of freestanding C, low confidence), so the single row became six slices in the order the findings give: memory, the disk and the record acts, clock and interrupts, the interpreter re-homed, the ledger green, the connection last. The single-writer-lock keeps its row; on a body that is the only writer by construction it is a within-body fact, a real lock returning the moment a second writer can exist. The code was deleted; nothing founded; the host and the guest's own kernel untouched. — §7 rows P3b-s, P3b-1..6; §9 Q9.
- 2026-09-12 · P3 (the kernel from the record) CLOSED, board :3997 (inner :3996) · The twelve kinds of work our kernel performs are now twelve rows of law in the founding (founding 1.55.0, pack 95631e8f, a new founding step of twelve CREATE-RULE records each carrying the kind, its portability and its meaning); the kernel's act set is derived from those rows at build and reconciled against every declaring site; deleting the derived set and regenerating it from the record returns the same twelve; at every waking the running kernel's declared set is judged against the rows after the two-body check, a planted divergence refusing the first act. The declared set stays a literal in the kernel's own code as the running body's declaration, so the record judges it rather than describing itself. Two reds the manager's re-gate caught (a second serializer, routed through the one canonical one; the cell census moved by the twelve rows, its evidence regenerated) were fixed in session. The kernel's body does not yet boot: P3b-s then P3b. — §7 row P3; §9 Q9.
- 2026-09-12 · P7a (the interpreter-hosting layer) CLOSED, board :3990 (inner :3989) · The layer that gives the Python interpreter what it needs (files, threads, memory, clock, entropy, the one connection, a C-library surface) now exists above the twelve kinds of work, memory the declared twelfth; the interpreter and its C library are consumed sealed, named by a pinned digest and re-checked at every waking, a planted byte refusing; a real CPython in the pinned guest ran through the layer (print(2+2) = 4). Attested members 57 (bridge/host_seam, hosting/__init__, hosting/interpreter_host); no founding; the stand-in beneath the layer is the hosted base, so what is proved is the layer's shape and that the interpreter runs through it, not yet that it runs on our own kernel, which is P3's acceptance. — §7 row P7a; §9 Q2 unchanged (the owner's at P5).
- 2026-09-12 · P4 (the wake checklist) CLOSED, board :3970 (inner :3960) · Before its first act by an actor, gov-os now records six verdicts as a genesis row: its body, its chain, its constitution read back, its views folded fresh, its keys, its account chain; any one failing refuses the first act; built on the Linux kernel today and carried to our kernel unchanged.
- 2026-09-12 · P6 (the genesis pair) CLOSED, board :3971 (inner :3962) · Two separate bodies now check each other before the first act and the genesis row carries both verdicts; one body alone cannot write it; a disagreement refuses the wake. In the guest the second body is a second image; the physical chip body is the owner's.
- 2026-09-12 · Q1 resolved on the owner's word, board :3972 · Our own kernel, from a blank page. The sealed-kernel assumption the paper ran on was the architect's drift and is struck; nothing built depends on it; the layer for the interpreter still comes first; the ARM question falls away.
- 2026-09-12 · The kernel's form, on the owner's word, board :3973 · Its definition is rows; its code derives from them at build; at waking it is checked against them after the two-body check and before the first act; delete-and-regenerate is the test, as the demo does for views.
- 2026-09-12 · The vault completion CLOSED, board :3977 (inner :3975) · On the owner's own form: the byte-level test set aside, the vault's four call points rewired from the Linux kernel to our one file with no functional change, the test put back on at the new contents; two property tests beside it. Every one of the sixty calls now goes through the one file.

- 2026-09-13 · PRE-FLIGHT of P3b-4f-i / 4f-ii / 4d / 4c, board :4131-:4135 · On the owner's word (pre-empt the stops instead of waiting for a builder to find them), the four queued rows were measured on the guest against the real C library and the real interpreter before dispatch. Four findings bind as by-name precisions (no re-hash): SYS_exit joins the served set (4f-ii); the image is listable (4d/4c) and serve.c:299's scan refusal retires at 4c; 4c's file list is measured (7 shared objects incl. libcrypto.so.3 via _hashlib, 28 .pyc, the locale/gconv/localtime files); never CR4.OSXSAVE (4f-i/4f-ii). Two of the four would have been stops (the hung join; the failed import). — §5 L18, §9 Q15

- 2026-09-13 · P3b-4m (the address space laid out for the program) CLOSED at both hands, board :4143/:4144 · The body is a higher-half kernel: its direct map at 0xffff800000000000 over a fixed 1 GiB, the identity map withdrawn from the program's range, 0x400000 user-mappable, the P3b-4a worker re-linked there and still unprivileged. Delta from :4126 (a): a fixed cap plus a visible FAIL above it rather than a report-sized map — the guarded property holds; residue named. — §7

- 2026-09-13 · P3b-4f-i (the static single-thread floor) CLOSED at both hands, board :4158/:4159 · An ordinary gcc -static glibc program (ET_EXEC at 0x400000, no base flag) runs to completion on the body: a real initial stack and the six-key auxv, a real address-space manager behind mmap/munmap/mprotect/brk, the main-thread %fs base, the machine's SSE enabled without OSXSAVE; the four plants fail the program by real faults. Nothing built in the earlier disk-killed spawn was lost (re-gated from the committed tree). — §7

- 2026-09-13 · P3b-4f-ii (the concurrency floor) CLOSED at both hands, board :4169/:4171 · An ordinary gcc -static -pthread program runs to completion on the body: real thread contexts by clone3, a preemptive scheduler switching %fs and the 512-byte SSE state only outside an act, a real futex queue, a thread's own end (SYS_exit, the fiftieth shape the census missed) with the child id cleared and woken so a join returns; the five plants fail the program by real fault, hang or corruption. The real program surfaced four latent defects in the crossing (register preservation, per-thread user stack pointer, the preempted state, the child's MXCSR) — the L19 outcome in practice. No snapshot taken (the :4152 discipline). — §7

- 2026-09-13 · P3b-4d (the dynamic loader and the sealed image) CLOSED at both hands, board :4190/:4192 · An ordinary gcc -pthread -no-pie dynamically linked program runs to completion on the body through the real dynamic linker loaded from the sealed, digest-checked image: PT_INTERP read, ld.so mapped at a base, the seven-key auxiliary vector, handover, libc resolved from the image by path; the image served by path AND by listing (Q15), ENOENT the only refusal outside it; the five plants fail the program by real mechanisms. No snapshot taken. The interpreter itself is P3b-4c, next. — §7

- 2026-09-13 · THE BODY MEASURED (board :4200, planning/evidence/C7-BODY-METRICS/) · 30 files, 4,921 code lines (enclosure.c 1,111, serve.c 777 — 38%); 49+1 served shapes, 47 planted breaks; symbol coupling: 42 edges, serve.c and kmain.c fan-out 9, serial.c a pure sink, three cycles all inside the crossing/server/interrupt knot (enclosure.c<->enclosure.S, enclosure.c<->serve.c, idt.c through them), the rest acyclic. For P8's numbers: size beside the small research kernels (~6-10k), a smaller declared surface, and the trail none of them has. A later row may cut the crossing/server cycle one-way if serve.c grows under the derived surface (design/57). — §8

- 2026-09-14 · P3b-4c THE CRUX closed at both hands for A1/A2/A5/A6 (board :4206/:4218): the real, unmodified interpreter runs on our own body from the one sealed image through the real dynamic linker; one fidelity fix (a sealed directory reports itself a directory); A3/A4 (gov-os's gate signing a row on the body) re-cut as P3b-4g before P3b-5. The four outside-round-three repairs closed at both hands the same night (OUTSIDE-5 :4219, -8 :4220, -6 :4221, -7 :4222/:4223). — §7
- 2026-09-14 · P3b-4g THE GATE ON THE BODY closed at both hands (board :4231/:4232): gov-os's own gate, imported as content off the record disk by the body-hosted interpreter, signed a record row on our own body with a guest test key, appended it through the record-pen act, read it back through the body-read act, and folded the crossing trail's digest as the act-witness row; verified off the body by the estate's shipping verifier, every plant a real failure. AS-BUILT vs target: (1) the signature state is MODELLED, named in the close and asserted by test (libsodium absent from the sealed image) — the real Ed25519 row is a measured addition, not claimed; (2) the gate reaches the record disk through the EXISTING served shapes routed to a second namespace (/rec/), so the served set did not widen (L18's fifty holds; zero case labels added); (3) the act-witness names the crossings up to the trail snapshot, written once at the first /rec/trail read — the gate's own later crossings stay in the unsigned trail, L18's floor as written. Sections: §7 row, §1 version.
- 2026-09-14 · P3b-5 STOPPED before any edit (board :4234) and RULED (:4235): the body serves no path outside the sealed image and /rec/, and its mkdir and rename answered success while doing nothing; the runnable census had counted a syscall's presence as a path's writability, and the architect's countersign and precisions carried that figure (the pre-flight measured call sets and file unions, never write classes). Target vs as-built: no tmpfs is built (L9, L21); test worlds become namespaces on the record disk served by the existing acts; the ledger's pass set is measured by write class first (5s2), then the namespace (5a), then the ledger (5b), then harness fixtures (5c). Sections: §5 L21, §7, §1 version.
- 2026-09-14 · P3b-5a first freeze refused (board :4244) on its own stop condition, found by reading before any build: the P3b-2 disk format holds 32 flat 32-byte names, one record entry by name, 32 KiB of record and 256 KiB of blob room, and never reclaims space, so caller-named worlds cannot ride it unchanged. Target vs as-built: the format grows (BODYFS02) rather than the body growing a memory scratch area; the plan's 'frozen by I7' was a mis-cite (I7 is the witness-class invariant); 5a is sized by the write-class measurement's counts and bytes, so it follows the findings. Sections: §5 L21, §7, §1 version.
- 2026-09-14 · P3b-6 STOPPED before any src edit (board :4246) with the device axis proven in a throwaway (our own virtio-net driver sends and receives on the wire; the planted dead device reds) and RULED (:4247): the plan had left the TCP stack to the builder's choice and the borrowed choice needs frames to cross to a ring-3 worker, a shape not in the interpreter's served set. Target vs as-built: the served set becomes PER ENCLOSURE rather than one list; the borrowed stack is the definitive form (L5) and a hand-rolled stack in the signed base is refused; the slice is split 6a (device, ours, GREEN, now) / 6b (the enclosed stack + its set) / 6c (the act wired). Sections: §5 L18, §7, §1 version.
- 2026-09-14 · P3b-5s2 measured at the architect's hand (board :4248, planning/evidence/C7-P3b-5s2-WRITE-CLASSES/): 193 modules traced with argument shapes; 81 pass as the code stands, 112 refused, none writing outside its world; the writer's lock (a truncate of the lock file to nothing) alone refuses 55 and is the program's own act through the seam, so it is served on the lock name; the damaged-tail sidecar is rewritten identically by the program, so write-once is by name and content; record resets and copystat are harness habits for 5c; subprocess and chroot stay out by facility. The census's 146 was the facility cut; the write-class cut is stricter and is the one that binds 5b. Sections: §5 L21, §7, §1 version.
- 2026-09-14 · P3b-6a closed at both hands (board :4252/:4253): our own virtio-net driver (src/body/net.c, attested by name, 31 body files / 88 members) brings the guest's network device up and the ring-0 check proves the wire, the guest gateway answering an address round trip; the planted dead transmit path fails for real; no served shape added. As-built precision: the check runs once at boot, polls, and leaves the rings to 6b. Disclosed beside it: four older body tests scan source text for the interpreter's name and red on comments, a location check; ruled to a GREEN maintenance unit re-pointing them to the property (:4254, L14). Sections: §7 row, §1 version.
- 2026-09-14 · P3b-6b pre-flight measured at the architect's hand (board :4263, planning/evidence/C7-P3b-6b-PREFLIGHT/): lwIP 2.2.0 exists in the guest's own archive (provenance met without a fetch); driven unmodified through an in-process loopback it echoes 8 KB through its own TCP and asks its host for four things after start (heap, entropy at malloc init, write, exit) plus the clock, two frame functions and a wait. Target vs as-built: the binary package is a prebuilt pthread-port shared object not built with its own headers (a receive-pool mismatch that overruns the heap), so the enclosed form is the static no-OS build from the archive's source package, with the stack's own example as the prover; the enclosure's served set is eight shapes declared as data (L18 per enclosure). Sections: §7 row, §1 version.
- 2026-09-14 · P3b-5a closed at both hands (board :4277/:4279/:4282, outer :4283), built as three HOW slices: the record-disk format is BODYFS02 (2,048 entries of 256 bytes with 128-byte path names, a 64 MiB data region sized from the manager's measured pre-flight, a BODYFS01 image refused at mount); under /rec/ the body performs the seam's own write acts at caller-named paths with mkdir and rename now real, write-once by name and content, the one-writer act on the marker name class only, a removed name re-creatable; the whole gov-os tree is one archive on the record disk behind the finder. As-built vs target: diskcheck.c took a mechanical widening of the walk; the P3b-2 test needed no edit because its pins read the format from the code. The owner's pause (:4281) took effect after this close. Sections: §7 row, §1 version.
- 2026-09-16 · P3b-5b countersign refused at the architect's hand (board :4288): the plan's A1 rule re-derives 136 exactly from classification.json, but 28 of them exited 1 in the measurement itself and 2 are the observe-watch and server-socket modules A2 declares out -- a candidate set carried as a pass set; the re-mint subtracts the facility names, settles each exit-1 module by its recorded cause on a named axis (content / facility / the standing red tree) before demanding green, names the 14 guest-driving modules as a declared class, drops the closed scans carry, cites the findings as ':4248 (second event)'. WHAT unchanged. Found beside it: four physical lines of the record hold two glued events each (:963, :4236, :4248, :4256) and the fold reads only the first -- ruled to C7-MAINT-BOARD-GLUED-LINES (:4289), GREEN: the gate terminates the log before appending, the fold reads every event on a line and names glued lines; the record itself untouched. §7 row P3b-5s2's citation corrected; §1 v0.76.
- 2026-09-16 · P3b-5b re-mint countersigned at the architect's hand (board :4294): the five refusal items folded and re-driven; test_ep51 found to import a release tool outside the staged src+tests archive, so it rides the content axis. Same day, P3b-6b stopped by the manager with zero edits (board :4290): the after-main measurement of the borrowed stack (four calls) was taken for the whole and the plan demanded exactly eight served shapes, but a static glibc worker issues seven more before main. Ruled (board :4295): the worker stays unmodified (L5/L19); the enclosure's served set is its whole-process measured set; six of the seven pre-main shapes are the interpreter's floor performers already in the body and the seventh (readlinkat) is the standing refusal-by-name a static program tolerates; the countersign :4267 struck; re-mint owed by the mentor.
- 2026-09-16 · P3b-6b re-freeze countersigned (board :4302): the borrowed stack's real whole-process set is twenty names, sixteen before main and four after, every one a performer the body already has for the interpreter; declared as this enclosure's own list, the frame shapes and the wait the only new performers. P3b-5b's first sub-slice stopped with zero edits (board :4300): the body can run only a compiled-in script, so the plan's run form named a mechanism the body does not have. Ruled (board :4303): the walking skeleton first, the Python-execution half inside 5b, and five precisions on the fidelity line -- the sealed image carries the interpreter's whole shipped standard library (measured 1,082 files, 33.1 MB, longest path 86 under the 104 cap), the module to run is data on the record disk, imports resolve through a general finder over the archive serving path and listing, temp worlds are namespaces under the record disk, and the result is the real test report on the serial line.
- 2026-09-16 · P3b-6b closed at both hands (inner :4315, outer :4317): the borrowed lwIP 2.2.0, built static from the guest archive's source package and unmodified, runs as a second enclosed worker with its own declared set of twenty shapes as data, and its own client opens one guest-local connection through our virtio-net device to a listener in the outer guest; the stack is sealed content, no borrowed function in the signed base. Disclosed on the way: the builder's non-answer, a red taken from a mid-edit tree, and the manager's premature revert of a completing build, recovered from the commit and re-gated on an idle lane; the lesson that a builder's liveness is the working tree, recurring. Caps carried to 6c: the client's exchange volume beyond A5's sanity band; the stack's own latent error path on a closed socket.
- 2026-09-16 · P3b-5b's second batch stopped by the manager with zero edits (board :4322): sixteen modules run on the body, none green, because gov-os's store opens a caller-named record for append and creates it on that first stroke, while the body serves creation and append as two separate acts and answers the combined one as absent. Ruled (board :4324): this is 5a's own row incomplete, not a harness habit and not a new act; a completion row 5a-iv serves the record pen's first stroke at caller-named names; 5b holds until it lands and its population is re-derived against what the body serves as built, the three tool-file cases routed to the content axis. The architect's own 106 disclosed as an exit-code figure, never a body figure.
- 2026-09-16 · P3b-5a-iv closed at both hands (inner :4335, outer :4337): under a world on the body, a record opened for append by a name the caller chooses is created on the first stroke and extended on later ones, truncation stays refused, and gov-os's own store act runs green there; three plants red by real mechanisms. Found at the outer close: the body's own disk self-check has printed a failing body-read line on every clean boot since the tree archive was staged on 2026-09-14, because the check pinned the disk to exactly three entries; no test read the line and four closes carried it unread. Ruled to a maintenance unit (board :4338): re-point the check to the property, and one standing assertion that a clean boot prints no failing check line.
- 2026-09-16 · P3b-5b's resumed batch stopped again (board :4341): seven modules green on the body, gov-os's store round trip among them, and eleven red in three classes that are the seam's own acts as gov-os performs them: the write-once by a temporary name truncated then swapped, which the body refused because it refuses truncation by flag rather than by the name being present; the writer's lock as an open-for-read-write plus a held exclusive claim, which the body served only as a truncate; and a fresh reader not seeing what a writer appended. Ruled (board :4343): the body owes the exact shapes of the seam's own performers, about twenty methods, a row P3b-5a-v; and the ledger's population is derived from the traces against those shapes, never from the census's classes. The lesson of :4234 at its last rung: a class served is not a shape performed.
- 2026-09-16 · P3b-5a-v closed at both hands (inner :4352, outer :4354): the body now performs the three seam acts as gov-os's store issues them -- the write-once by a temporary name truncated then swapped over its final, the writer's lock taken and held on the marker file, and a fresh reader streaming a caller-named record of any size -- each proven inside the body-hosted interpreter by the real store method and each with a plant that reds by a real mechanism; the derived population of modules whose every write the body now serves is 136, read from a list. The manager re-gated everything at its own hand after the guest was restarted (the guest had swapped out under too many nested boots at once; one builder at a time from here). The architect's outer read found one thing built against the countersign: a second lock claim from the same process is refused by the body where Linux grants it, and the test asserts the refusal; gov-os never makes that second claim (the store demotes a second same-process instance to a reader first), so nothing is red today, but the body's shape is not the host's and the plant fires on correct behaviour -- ruled to the green maintenance unit C7-MAINT-LOCK-POSIX-PROCESS-CLAIM (:4355): grant the same-process claim, record the cross-process refusal as not provable on a one-process body, replace the plant with one that can fail. The interpreter-on-body regression's completion stands as the manager's testimony; its lines are owed to the evidence file. -- §7 row P3b-5a-v; §1 version.
- 2026-09-16 · P3b-5b's second pass stopped by the manager with zero edits (board :4358) and ruled at the architect's hand (board :4360): twelve modules run on the body after the three seam acts were served; eight green, and the blob write-once and reconstruct-from-record classes confirmed green by the real modules that were red before. Four red, all in the lock class, and none a lock defect: the body's open-file table is forty-eight wide, sized for the loader before the interpreter ran on it, and gov-os's store holds one lock file open for each live writer by design, so the modules that build many kernels run past the table where Linux allows 1024; the body fills every clock request from its since-boot counter, so the recording clock on every row reads 1970 and the two-times rule refuses decisions dated 2026, while the body already reads the real-time clock and never serves it as a timespec; and a world's blob directory is declared as a flat name, so a listing of it answers not-a-directory. Ruled: all three are the body's to serve as Linux serves them, none is a test to declare away or a store to change (the campaign's first line, nothing above the seam changes), and the write-class census's population rule is necessary, not sufficient -- ceilings, the time axis and directory depth are further axes of served exactly. Minted P3b-5a-vi, yellow, sized from measurement (the per-module maximum open file from the traces; the RTC the body has; the store's real blob paths), with the four red modules as its provers; 5b holds on it and resumes with those four first. The manager's offered store-lifetime rule refused as above the seam. -- §7 rows P3b-5a-vi, P3b-5b; §1 version.
- 2026-09-16 · P3b-5a-vi countersigned (board :4362) on the mentor's freeze, which adopted the ruling in full and withdrew its own re-measure boundary. Premises read in the body: the resource-limit answer today writes the stack pair for whatever resource is asked, so the body reports an open-file ceiling it does not hold and the unit makes the report true; the clock file already decodes the full civil date with the century, so the epoch needs only a days-from-civil conversion; six arrays are sized by the table width and fit at Linux's width. One precision rides the countersign: the scheduler's timed wait must compare the real-time deadline against a now carrying the same epoch offset, or convert it back, else every timed wait expires never or at once; the earlier concurrency regression proves it. Record structure: the board's line 4361 held the mentor's two events glued with no final newline (the glued-lines defect live again while its repair is queued); one bare newline was appended, never a rewrite, so later events land on their own lines; the fold sees only the first event of a glued line, so the freeze is visible in the text and on the countersign, not in the fold. -- §7 row P3b-5a-vi; §1 version.
- 2026-09-17 · P3b-5a-vi built and stopped (board :4369), P3b-5a-vii minted and countersigned (board :4370, :4371): the three axes were built and each proven by the module that had failed on it (the open-file table at Linux's width with the peak demand derived from the traces as 203; the real-time clock from the emulated real-time clock with the concurrency coupling kept; the blob directory as a directory with no change to the disk format), and the heaviest module then ran far enough to find a fourth: the body's unmap act clears the page's mapping but never returns the frame to its allocator, its own comment saying so, and its map act never reuses an address, so the pool only shrinks under churn; the forty-eight-wide table had been capping that churn by accident. The mentor ruled a fresh unit rather than amending a built one, and the architect signed it with three precisions from reading the code: no frame is ever mapped at two user addresses (the fixed overlay reuses the frame under the same page; the sealed image is copied, never mapped), so the guard is the page entry itself, not a per-frame ownership record; a plain double-free plant cannot fail because the allocator's free ignores a clear bit, so the plant must free a stale frame and corrupt the dynamic loader; and page-table frames accrue under the address bump and are measured in the close, not reclaimed by this unit. -- §7 rows P3b-5a-vi, P3b-5a-vii; §1 version.
- 2026-09-17 · P3b-5a-vii built on the unmap side and stopped (board :4373), re-minted to extend and re-countersigned (board :4374, :4377): the unmap act now returns each frame it owned, freeing only a page that read present and user-owned before the unmap, with one plant that puts the leak back and one that frees a live neighbour's frame so the dynamic loader corrupts; that moved the heaviest module from five memory failures to four and drained the pool to nothing, which exposed the second leak: the break act rounds both ends up to a page, skips its loop when the break shrinks, and lowers the break with the shrunk pages still mapped, so the next growth maps fresh frames over them and the old ones are lost on every one of CPython's grow-and-shrink cycles. The mentor extended the unit to free the shrunk pages by the same rule rather than mint a sibling; the architect signed with two precisions: the growing side maps a fresh frame only over an absent page, so no path in the break act can lose a frame again; and because the ledger body itself loads CPython through the real dynamic loader with fixed overlays and gap unmaps, every module green on the built body is evidence for the loader's safety, so the earlier unit closes and the ledger run resumes on the heaviest module green, while this unit closes only once its loader plant is verified on the dedicated dynamic prover. That prover was found not to boot on the original body either, halting after paging is enabled, the manager's own measurement and its disclosure that the pre-flight had not booted it; a diagnosis spike is minted (board :4375) and may gate the socket slice if it shares the dynamic-boot path. -- §7 row P3b-5a-vii; §1 version.
- 2026-09-17 · The dynamic-loader prover's boot halt found and repaired in plan (spike closed board :4379; unit countersigned :4382): the prover embeds its sealed image of the loader and the C library into the body's own image, and as the body's static buffers grew through the 5a series and the network code, the body's end-of-data crossed the 4 MiB canonical base that the higher-half layout withdraws from the supervisor map, so the first global write after paging faults before any interrupt table exists and the boot halts; the interpreter body and the ledger body never halted because they carry their image as a boot-time module read through the direct map. Extending the identity map was tried in the spike and rightly rejected: it re-creates the conflict the layout law exists to prevent. The repair carries the prover's image the way the interpreter's already is, adds a build-time check so a body over the base fails the build instead of faulting silently at boot, and keeps the whole-image digest check on the module bytes so the seal travels with the carriage. The socket slice is not gated: its body stays well beneath the base. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · The dynamic-loader boot repair closed at both hands (inner :4394, outer :4396): the prover's image travels as a boot-time module read through the direct map and digest-checked as a whole before load, the body's end-of-data fell from 4.457 MiB to 2.199 MiB, the seal is byte-identical before and after, the twenty-test dynamic-loader module passes with its five older planted faults still failing on the module path, and a body linked over the base now fails the link with a named message while an under-base one links. Correction of record: the plan said the build script and the linker script are not signed members; both are attested, only the test is not, and the unit stayed an in-place edit of members with the set unchanged. One bound named: the linker check pins the literal four-megabyte value, since a linker script cannot read the header that defines it; a one-line pin that the two agree is owed to the standing-test unit. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · P3b-5a-vii's shrink reclaim built and the heaviest module green with its pool sustained (the manager's measurement); the same measurement showed the module's peak is heap-bound, so the test that puts the unmap leak back leaves it green and cannot fail. The mentor re-homed that test onto the dynamic-loader prover (board :4384); the architect refused (board :4385) because that prover does not map and unmap either, one small allocation and two cached thread stacks, so the leak would be a few megabytes once inside a pool of two hundred and fifty-six. The rule that holds: a test that puts a fault back must be run by a program that actually exercises the thing, chosen by measurement, not assertion; the traces from the write census carry no unmap lines, so the churn is measured on the guest, a ledger module first, and only if none churns past the pool an ordinary real-libc program whose large malloc and free glibc itself serves by map and unmap. The unmap fix stays as built; only the test's home moves. -- §7 row P3b-5a-vii; §1 version.
- 2026-09-17 · P3b-5a-vi closed at both hands (inner :4386, outer :4389): the open-file table at Linux's width with the derived peak in its own comment, the real-time clock as an epoch base taken once from the real-time clock and advanced by the same tick as the monotonic clock so the two never drift, the concurrency deadline converted back into the scheduler's frame by the same base, and a world's blob directory as a directory entry that stats and lists; the four modules that failed on them pass, the heaviest only with the frame reclaim, so the close rests on both units. The architect's outer read found the unit's own standing test never written: the three axes are proven by serials a builder ran by hand and asserted by nothing the ledger runs, the same class as the body-read check found unread for two days; ruled to a green maintenance unit that delivers the test as specified (board :4390). The frame-reclaim unit's third re-mint, homing the unmap test on a prover chosen by measured churn, is signed (board :4391). -- §7 rows P3b-5a-vi, P3b-5a-vii; §1 version.
- 2026-09-17 · P3b-5a-vii's unmap test found its home by measurement (board :4397, :4398, :4400): a census of all 114 test modules run the way the body runs them showed none releases more than about thirty megabytes in-process (the eight figures over the pool were a trace-follow artefact of subprocesses the body cannot have), so an ordinary C program under the real C library, two threads then sixty-four rounds of eight-megabyte allocate-and-free above a fixed mmap threshold, is the prover, and with the reclaim disabled it runs out of memory at round twenty-nine. The plant for the page-table check was then found inert on the dynamic-loader prover for the very reason that prover was not the churn prover; the mentor moved it to the churn program, and the architect signed with the plant re-formed: reading the code, the neighbour-aliasing fault cannot show on any ordinary program because every freed page's neighbour is a page of the same chunk or the guard page between maps, whereas what the check truly protects is the reserved set of frames, the low megabyte and the body's own image, which an unguarded free of an absent page would return to the pool; so the pool's free count is the invariant, an ordinary second unmap of an already-freed range is the program's act, and the body's own count line over-running is the failure the test reads. -- §7 row P3b-5a-vii; §1 version.
- 2026-09-17 · P3b-5a-vii closed at both hands (inner :4404, outer :4405) and with it the whole 5a series: the body returns frames on an explicit unmap and on a shrinking heap, guarded by the page entry; the heaviest module green with its pool never exhausted, driven through the verifier; the unmap test on a real-libc program churning 512 MiB, out of memory at round twenty-nine with the leak put back; the dynamic-loader program green on the module carriage as a witness. The page-check test fires by real corruption: the builder mapped two pages in one call, kept the second as a painted sentinel, unmapped only the first, and the unguarded body freed the sentinel's live frame and handed it to the next map, which overwrote 4080 of the sentinel's 4096 bytes. The architect's outer close discloses its own error: the signing precision had said no ordinary program could reach that aliasing, having read the whole-chunk case and not the partial unmap; the mentor's corruption form was right, and the lesson the architect had given the mentor the same day, choose by measuring not by asserting, applied to the architect. The standing-test unit is read (board :4406): the three ceilings-and-clock axes are asserted by the ledger on every run; the one-line agreement between the header's canonical base and the linker's literal is still owed to the next body maintenance. -- §7 row P3b-5a-vii; §1 version.
- 2026-09-17 · P3b-5b's third pass (board :4408): the four modules that had failed on the ceilings, the clock, the blob directory and the memory reclaim all pass on the body, twenty-five more pass, two are all-skip, and ten fail in two classes: six read the conformance instrument under tools/, which the mentor ruled outside the ledger's content (board :4410), and four are body shortfalls. The mentor's unit for three of them was refused at the architect's hand (board :4412): the go-up path normalization is right; but the removal failure is not a link mode, it is the standard library's own check that a directory's identity is the same from a path stat and from its opened descriptor, which the body answers with one constant for every directory by path and a per-slot number by descriptor, and the same per-slot identity is what the descriptor-identity test needs per file; so the axis is one, stat identity as a property of the file as Linux gives it, and with it the removal of a temporary world under /rec/tmp/, its record and its emptied directory, ruled served as the seam's own remove act. The third fix, returning a true byte count from the old canned record path, was refused as a stub answering success for a write it never performed; an unknown descriptor answers as Linux does. The append-refusal on the fourth module was measured by the manager (board :4411) as the single per-module record disk filling under ten-plus worlds in one boot, a capacity ceiling of the same family as the open-file table; the architect's first reading (a name typed by its first act) was wrong and withdrawn, and the fix ruled a sibling unit sized from measurement (board :4413). -- §7 row P3b-5b; §1 version.
- 2026-09-17 · The record-disk sizing unit refused at the architect's hand (board :4415): reading the append path showed that a world's record which is no longer at the allocator's tail is copied whole to the tail every time it grows, and its old sectors are never reused, so a module appending one row at a time across several live worlds consumes the disk as the sum of its record's successive sizes, not as the bytes it wrote; the heavy module's failure is that shape. Sizing the region from bytes written, as the unit proposed, would be the same under-counting proxy it corrects for the path count. Ruled: the entry table from the measured entry count; the data region from the measured consumption read off the body's own superblock; and the root, a relocating record claiming capacity by at least doubling so relocations become few and the dead space stays bounded, with no change to the disk format since capacity is already a field of the entry. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · The serve-corrections unit re-written on the architect's causes and signed (board :4419): the go-up path normalization with an escape refused; a file's identity as a property of the file, the same number from a path look, from any open handle and from a re-open, with the removal of a temporary world under /rec/tmp/ served and every other removal refused by name; and a write on an unknown handle answering the real bad-handle error with the canned length retired. The architect added that the identity number must be the entry's table index and never its start sector, because the allocator moves a record's start when it relocates, so a start-sector identity would change while a store held the file open. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · The record-disk capacity unit re-written on the allocator finding and signed (board :4422): a world's record that must move to grow now claims at least double its need, so it moves a logarithmic number of times and the dead space it leaves stays bounded by about one copy of itself; the data region is sized from the disk actually consumed as the body's own bookkeeping reports it after the heaviest module runs, with the bytes written recorded beside it; the entry table from the measured number of entries; no reclamation of dead sectors yet, a cap on the record; and no change to the disk format since the capacity is an existing field of the entry. The architect added that growth in place at the disk's tail stays as it is, since it wastes nothing, and that the fixed record, the tree archive and the body-read check are untouched and shown passing on the resized disk. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · The three serve corrections closed at both hands (inner :4424, outer :4426): the path finder resolves a go-up step and refuses a climb above the served root; a file's identity number is its entry's fixed slot in the table, the same from a path look, from any open handle and from a re-open, and a handle the body does not know answers as absent rather than as a character device; a discarded world under the temporary area can be emptied and removed, its record included, with a non-empty directory refused as Linux refuses it and every other removal refused by name; and a write on an unknown handle answers the bad-handle error, the canned length reachable only under its own plant. The four modules that failed on these pass; the four planted faults each fail by their own mechanism. One bound named: a tree directory's identity is a fold of its path, so two could in principle share one, which nothing reads today since tree directories are read-only and never compared for sameness. -- §8 only (a maintenance unit); §1 version.
- 2026-09-17 · The heavy module's failure measured at last (board :4427): the builder read the body's own bookkeeping at the halt and the disk was seventy-two percent empty with sixteen of two thousand and forty-eight entries used, so it was never capacity; the module's setup opens its record for write on an absent name and closes it, the body types that empty file as a staging file for the durable write, and the store's later open for append refuses it as a foreign class. The manager's capacity reading, the mentor's two write-ups on it and the architect's quadratic sizing all stood on the module's shape and a code comment, none on the failing state; the architect had named the true mechanism the day before and withdrew it for the manager's inference. The reconcile unit is signed (board :4431): an unswapped staging file opened for append becomes a record in place, its bytes kept and the append at its tail, the durable temp-then-swap distinct by act, the truncation refusal standing. The geometric-growth hardening is deferred by the minimality gate with a measured trigger (board :4432): the walk records the disk consumed per module from the superblock, and the unit is pulled forward only if a module's consumption passes half the region or fails out-of-space far below its byte total. -- §8 only; §1 version.
- 2026-09-17 · The fourth pass of the ledger on the body surveyed (board :4440): forty-nine of the remaining sixty-four modules pass; seven read the verification tooling or the design papers and are declared outside the record's content, three need a facility the body does not serve, one shares the open-class cause already in repair, and three body shortfalls remain: eight thread slots where the group-commit tests start more, a current-directory answer that returns a code and never fills the caller's buffer, and an import by package that needs the record root on the search path. The unit for the three is signed (board :4444) with three precisions: the record root itself must be served as a listable directory or every import fails once it is on the path; the slot count has a ceiling from the four-megabyte layout since each slot is a sixteen-kilobyte stack in the body's own image; and the directory answer fills the buffer and returns the length with its terminator, as the kernel contract says. -- §7 row P3b-5b; §1 version.
- 2026-09-17 · The open-class reconcile closed at both hands (inner :4446, outer :4447): an empty staging file created by a plain write-open and then opened for append becomes a record in place, under the temporary area only; the durable temp-then-swap never opens its staging file for append and stays distinct; the two heavy modules pass; the first plant proves the family by its error number, a class refusal on an empty disk, never out-of-space. One bound named: a staging file with bytes in it, closed and then opened for append, is still refused, a shape no module of the walk shows. -- §8 only; §1 version.
- 2026-09-17 · The thread-slot unit falsified at the build (board :4452): the body's scheduler marks a finished thread a zombie and never frees its slot, so a boot's demand is the count of every thread ever created, fifty-seven on the heaviest module against a peak of sixteen, and no table size honestly covers that; the getcwd and import-path halves were split into their own unit and signed (board :4456). The architect ruled reclaim (board :4457): a finished thread's slot is freed at the first safe point after it switches away, by whoever runs next, once its exit has cleared and woken the join, detached threads included, so the table bounds peak concurrency and is sized from the measured peak; the exit counter, the join and the timed wait unchanged so every concurrency pin stands. The campaign's must-not-touch list never named the scheduler; that wording was a plan's own guard, swapped with its property kept. -- §8 only; §1 version.

## 9. OPEN QUESTIONS / OWNER GATES

- Q1 THE CORE'S PROVENANCE — RESOLVED :3972: our own kernel. The ARM/x86 fact no longer bears; P7a's shape is fixed by our own kernel's primitives and its detail is released.
- Q2 THE INTERPRETER (one-way door, the owner's): the interpreter cannot be read whole; consume it sealed and attested under a named exception to L2 (the architect's position, :3946), or move the gate to a readable runtime in a later campaign. His word at P5's held step. Not blocking now.
- Q3 THE CHIP BODY: the physical second witness is hardware and the owner's; in the guest a separate image stands in for it (P6). Not blocking.
- Q4 THE TRANSLATOR'S HOME — RESOLVED :3919: C7. The device list is new to C7 (C5's remainder is ungoverned-peer identity, C6's) and is DERIVED from the effect seam: the guest's block device, network device and clock source, nothing more until a rule names more. P7's size is stated at P2's close.
- Q5 THE CLAIM: the public status page says "hosted" today and changes one word at B1. The owner's word on the page still waits (planning/evidence/OUTSIDE-ASSESSMENT-2026-09-09/STATUS-PARAGRAPH-DRAFT.md). Not blocking.
- Q7 THE VAULT AND THE SEAM — RESOLVED on the owner's word of 2026-09-12: the byte-level freeze is set aside for the rewiring of the vault's four call points from the Linux kernel to our new file, no functional change, and put back on at the rewired contents; the two property tests stay beside it.
- Q11 THE MEMORY ACT'S SHARING — RULED provisionally by the architect 2026-09-12 (board :4043) on the P3b-4s measurement, confirmed at P3b-5s: the Linux performer of the memory act hands out memory shareable across a fork by default; the act's row promises host-backed anonymous memory and nothing about fork; our body is single-writer, threaded, with no fork, so it serves the act as anonymous memory private to the body. If the ledger's measurement finds a path that relies on fork-sharing, that path is named and declared, never served by adding fork to the body. CONFIRMED at P3b-5s: fork appears in the ledger only as subprocess, already declared out.
- Q10 HOW THE C BODY IS READ WHOLE AND SIGNED — RULED by the architect 2026-09-12 (board :4009) on the mentor's mechanism raise at P3b-1. The law is I3 / design/47 §2: no opaque code in the signed base, the body small enough to read whole and sign. The attested set's '.py' wording described what src/ held; it was never the law. So: ONE attested list — the body's C, assembly and build files under src/body join ATTESTED_MEMBERS by name beside the derive-from-rows generator, the one digest covers them, the same grow-only discipline, and the guard that catches an unrecorded file under src/ is extended to the body's file kinds; no second plane, no new count-pin kind. Named honestly: the memory code is hand-written C read whole and signed, not generated; the generator derives from the rows what the rows determine (the body's declaration and digest, any act table); delete-and-regenerate binds the generated files byte-identical; the built body's digest and toolchain are recorded at every slice, and byte-reproducibility of the built body becomes binding at P3b-5, where the two-body pair checks the body's bytes at boot.
- Q9 THE P3 SPLIT — RULED by the architect 2026-09-12 (board :3993) on the mentor's scoping raise (C7-P3-KERNEL-FROM-RECORD.md §9): the §7 row had mapped P3 to B1 and B6 while the owner's bar (:3973, L16, B11) is delete-and-regenerate of the kernel's definition. Two behaviours, two rows. P3 flips B11 (definition as rows, derived at build, checked at waking) and is buildable in one session in main; P3b flips B1 (the body booting in the guest, no Linux beneath) after a sizing spike P3b-s, the thinnest path being one line on the serial line from a body generated from the rows; B6 for the interpreter is P5's. Named so that no reader takes P3's close as 'our own kernel exists': after P3 the kernel's DEFINITION is rows and provably regenerates; after P3b the kernel's BODY boots. Two precisions ride the countersign by name: (a) the act-kind rows are rows of LAW — one CREATE-RULE record per kind in a new founding step, carrying the kind's name, its portability label and its meaning — never CREATE-OP (an act kind is not an operation) and no new operation; the derived act set is a fold over those rules; (b) the kind's label on its row is checked against every site's declared label at derivation (uniform per kind at HEAD, driven: 30 sites, 12 kinds, no disagreement), a disagreeing site caught. The close's rider (board :3997): the declared set in the kernel's code is NOT rebound from the record at build — it is the running body's own declaration, judged by the rows at waking; rebinding it would make the waking check compare the record with itself, a check that cannot fail. The P3b-s findings (board :4005) cut P3b into six slices and fix the single-writer-lock's shape at the metal (a within-body invariant on a single-writer body, a real lock when a second writer is admitted); the twelve rows are unchanged.
- Q8 THE WAKE CHECKLIST'S WIRING AND THE UNRECORDABLE GENESIS ROW — RESOLVED by the architect 2026-09-12 (board :3985) on the manager's two raises at P4's inner close (:3960), carried late. (a) The tested mechanism IS P4's deliverable (B4: six verdicts as a genesis row before the first actor act, each able to refuse). Wiring it into every composed world on the Linux kernel would run six verdicts over thousands of test worlds for no guarantee gained, since the performer beneath is not ours; on our own kernel the waking IS the boot, so the wiring belongs to P3, after the two-body check (owner :3973), and P3's re-mint carries it as a named premise. (b) A world whose record cannot fold cannot record its own genesis row: inherent, and the refusal still holds (the first act is refused with the failed names carried). It is witnessed anyway: under P6 the second body records its verdict on the pair over its OWN record file, so a body too corrupt to write is still recorded as refused by the body beside it. No code change; no founding.
- Q6 A FRESH FIRST-KEY WORLD (C6a Q7) is a ceremony and stays the owner's; C7's wake checklist reads keys from whatever world it boots. Not blocking.
- Q12 THE BODY'S MODE — RULED by the architect 2026-09-12 (board :4078) on the P3b-4 stop (:4076). The sizing spike's Q-A named the one real fork at start-up (32-bit protected mode as the probe stood, or 64-bit long mode) and wrote "that is a P3b decision, not a probe cost"; no seat ruled it, and P3b-1, P3b-2 and P3b-3 were authored, countersigned and closed in the probe's 32-bit form while the interpreter they exist to carry, and the forty-nine requests measured at P3b-4s, are 64-bit. The defect is the architect's: a spike's "this is a decision" line must become a ruling before the first slice that inherits it, not after the fourth. Ruled L18; the re-basing is P3b-4L. What the ruling refuses: rebuilding the interpreter to fit the body (the borrowed thing stays unmodified, L5; and the interpreter's provenance is the owner's, Q2), and proxying the interpreter's memory and threads to Linux while calling the body the performer (Linux beneath two of the twelve acts, by name). Not an owner gate: the mode is code behind a fence (I6, L8).
- Q13 THE PURPOSE-MODEL — RULED by the architect 2026-09-13 (board :4104) on the P3b-4c stop (:4102). P3b-4b's acceptance let OUR hand-written assembly worker be the prover of 'the forty-nine served': it asked in the measured shapes and accepted any answer; the body classified each request correctly and answered several with canned values (the thread-local base never installed in the machine register, a fake child id for a thread never created, a wait returning at once, a fixed address for every map, a constant break), and the checks read flags set by the same code. The countersign (:4089) and the outer close (:4100) were the architect's, reading those flags; the builder built what the acceptance could red. The defect is the fleet's oldest — a check that cannot fail — at the slice where it costs most. Ruled L19; the re-cut P3b-4f → P3b-4d → P3b-4c. The stand-in reading (a worker enacting the interpreter's run under the interpreter's name) is REFUSED: it is the record-holds-an-act-nobody-performed failure this estate exists to prevent, and no re-wording makes it honest. Not an owner gate: the campaign's outcome (B1, L9) is unchanged and the mode of proof is the seats'; the owner is told the cost and holds the standing revert.
- Q14 THE ADDRESS-SPACE LAYOUT — RULED by the architect 2026-09-13 (board :4122) on the 4f-i stop (:4120). The body identity-maps the low gigabyte as supervisor huge pages and maps user pages only above it (the P3b-4a worker linked at 1 GiB for that reason); an ordinary program, and the interpreter itself, loads at the canonical base inside that gigabyte and cannot be moved. The manager's options — link the prover high, or build it position-independent — would make the prover pass on a body the interpreter still cannot load on: exactly the fit L19 forbids, hiding the collision until the last slice. Ruled L20 and the row P3b-4m: the body is laid out to fit the program (its image beneath the base, its memory reached through a high direct map, the low range the program's). The miss is the architect's: two countersigns (P3b-1, P3b-4L) never asked where the hosted program would live. Not an owner gate: layout is code behind a fence (I6, L8).
- Q15 THE CENSUS'S 'DESIGNED REFUSAL' — RULED by the architect 2026-09-13 (board :4134/:4135). The P3b-4s census classed the module-directory scan as a live-filesystem scan the body refuses and CPython tolerates; measured under -I -S, the tolerance holds for site's scan only — importlib lists each sys.path directory once and fails every import when the listing is refused. Ruled: the listing of the sealed image is a read of the image and is served; the refusal is any path outside it. The census also missed SYS_exit. Both are the architect's: the countersigns of 4b/4d/4c carried the census's classification without measuring the tolerance it asserted. Cost: two precisions, no re-freeze; revertible.
- Q16 WHOLE-GUEST SNAPSHOTS FOR KERNEL-MODULE LOADS — OWNER GATE, opened 2026-09-13 (board :4152). The shim rows (tests/test_ep29.py W3b) protect the pinned guest before each govshim load by a whole-guest `savevm`; 186 undeleted snapshots grew the image to 339G and stopped the estate. The permanent fix (the test retires its own snapshot; a ledger guard reds on accumulation) stands either way; the question that is his: keep whole-guest snapshots for module loads, or run those loads in a throwaway overlay guest backed by the pinned image (no snapshot ever written into the live image). Not blocking; VM management is his by his own ruling of 2026-09-13.

## Register

SOURCE-DERIVED where cited to a dialogue record, design/47, or a board line; PROPOSED where marked (L9, the plan list's sizes). caps: written at one hand before C6a closes and before P1's census, so every size is an estimate at Low confidence; the seam's real width and the device list are the census's to state; the rider's charter wording is cited by its dialogue record (D07.12), not quoted.
