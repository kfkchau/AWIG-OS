"""gov-os founding — the constitution's source document (the pack) and its installer.

design/31 J9: the founding becomes the corpus's own boot step 1. `founding-pack.json`
is the data document (the nineteen root laws in full form WITH scope, the vocabularies,
the actors/tunnel/mother-space, the core ops, the masters, and the founding openness
grant); `install.py` is the minimal installer that executes it. Genesis-by-Python-
literals retires: boot.genesis() now delegates here.
"""
